---
version: alpha
name: TauX-design-system
description: 拓思科技官網的設計語彙。暖白紙表面、Charcoal 主墨版三階密度、Cobalt 副墨版承載所有互動。兩塊墨版是上限，樣式表裡每個顏色都必須是其中一版的密度階或表面本身。SF Pro 句首大寫標題配中文副標，章節以只放標題的區塊分隔，正文連續。沒有照片、沒有網點、沒有第三個顏色。

colors:
  # 表面（紙）。不計為墨版。
  canvas: "#fafaf7"
  # 主墨版 Charcoal，三階密度。名字寫的是「覆蓋率」不是「不透明度」。
  ink: "#30343a"        # 100%
  ink-86: "#4c5054"     # 86%
  ink-72: "#696b6f"     # 72%
  # 副墨版 Cobalt。只承載互動，沒有第二個用途。
  primary: "#2148b8"
  # 從墨版裡挖空看到的是紙，所以這個值等於 canvas——是推導出來的，不是巧合。
  on-primary: "#fafaf7"
  # 分隔線是主墨版的低密度階，不是獨立的灰。
  hairline: "#e2e2e0"   # 12%

typography:
  # ⚠️ 這一段的名字先前整排錯位一格，見決策 #87。
  # 下面用的是 tailwind.config.js 實際宣告的名字。
  display-lg:
    fontFamily: "SF Pro Display, system-ui, -apple-system, {cjk}, sans-serif"
    fontSize: 64px
    fontWeight: 600
    lineHeight: 1.07
    letterSpacing: -0.005em
  display-md:
    fontSize: 40px
    fontWeight: 600
    lineHeight: 1.1
    letterSpacing: 0
  display-sm:
    fontSize: 34px
    fontWeight: 600
    lineHeight: 1.18
    letterSpacing: -0.011em
  display-sub:
    fontSize: 24px
    fontWeight: 400
    lineHeight: 1.4
    letterSpacing: 0
  lead:
    fontSize: 21px
    fontWeight: 400
    lineHeight: 1.48
    letterSpacing: 0.011em
  body:
    fontFamily: "SF Pro Text, system-ui, -apple-system, {cjk}, sans-serif"
    fontSize: 17px
    fontWeight: 400
    lineHeight: 1.47
    letterSpacing: -0.022em
  body-strong:
    fontSize: 17px
    fontWeight: 600
    lineHeight: 1.47
    letterSpacing: -0.022em
  caption:
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.43
    letterSpacing: 0
  eyebrow:
    fontSize: 12px
    fontWeight: 600
    lineHeight: 2.0
    letterSpacing: 0.09em

rounded:
  none: 0px
  xs: 5px
  sm: 8px
  md: 11px
  lg: 18px
  full: 9999px

spacing:
  xxs: 4px
  xs: 8px
  sm: 12px
  md: 16px
  lg: 18px
  xl: 24px
  xxl: 32px
  huge: 48px

breakpoints:
  mobile: 600px
  tablet: 768px
  laptop: 961px
  desktop: 1280px
  wide: 1500px
---

# TauX 設計規範

這份文件是**設計的唯一來源**。顏色、字體、字距、圓角、間距、元件與動態，全部在這裡決定；`NOTES.md` 不再描述設計，`src/input.css` 與 `tailwind.config.js` 的註解只解釋實作細節，不定義規則。

**規則寫了沒有東西檢查，就等於沒有規則**，所以這份文件的每一條都盡量寫成 `npm run check:design` 檢查得到的形狀。

> **這份文件自己也犯過同一個毛病。** 初稿宣稱 `adk-skill-patterns.html` 有 20 個 hex 值，那是把 `&#123;` / `&#125;`（為了 minijinja 而轉義的大括號）誤讀成顏色。實際數字是 **0**，全站皆然。目視得出來的數字也要用工具數過。
>
> **v3 也犯了一次。** 品牌重置的分票初稿把封面屏數量寫成 **106**，那是全站 `<h2>` 的總數；其中 **6 個在 partial**。實際是 **100**，而照 106 寫出來的規則會要求 footer 的導覽欄長出 6 個滿版屏。
>
> **v4 的教訓不是數字，是來源。** 那一版採用的參考站把 `ink-muted-48` 訂成 `#7a7a7a`。那個值在白底是 **4.29:1**，**低於 AA 的 4.5**——照抄會讓 `contrast` 閘門第一次執行就紅。
>
> **v5 的教訓是名字。** 上一版的字級表寫 `hero-display 56 / display-lg 40 / display-md 34`，而 `tailwind.config.js` 宣告的是 `display-lg 3.5rem / display-md 2.5rem / display-sm 2.125rem`——**同一把梯子，名字整排錯位一格**。三組行高與字距逐項比對完全相同（1.07/−0.005、1.1/0、1.18/−0.011），所以那不是兩把梯子，是一把梯子兩套名字。上一版只抓到「`hero-display` 這個類別不存在」，沒抓到底下兩階也是錯的。**一個名字錯，看起來像筆誤；三個名字連續錯位，那是抄的時候整欄下移了一格。** 見決策 #87。

---

## 這套語彙的來源

參考站換成 **`yanliudesign/mono-color-skill`**——一套單／雙色編輯印刷的生成規範，取代先前的 Apple 網站設計分析。

它與前兩個參考站有一個根本差別：**前兩個是網站，這一個是印刷品。** 這件事貫穿整份文件，凡是「本站不採用」的理由，幾乎都回到這一句。

### 裁決規則——維持「參考站為準」

決策 #53 把裁決規則從「本站現行規則為準」反轉回「參考站為準」，本次維持。新檔沒有涵蓋的地方，才由本站推導補位。

⚠️ **這一版的「沒有涵蓋」比前兩版大得多。** 印刷規範不談斷點、不談 hover、不談 focus ring、不談圓角、不談五個 locale。**推導補位這次不是補洞，是撐住半份文件**——凡是由本站推導的段落都標了「〔推導〕」，因為它們的權威等級低於參考站直接給的。

### 這次換掉了什麼

**這是這份文件生涯的第三次品牌重置**，而三個參考站兩兩互相矛盾。授權紀錄見決策 #74。

| 項目 | v3（航太） | v4（Apple） | v5（mono-color） |
|---|---|---|---|
| 表面 | 純黑 `#000000` | 純白 `#ffffff` | **暖白紙 `#FAFAF7`** |
| 顏色的組織方式 | 沒有 | 五個獨立 token | **兩塊墨版 + 密度階** |
| 墨色 | 純白，一階 | `#1d1d1f`/`#333333`/`#707070` | **Charcoal `#30343A` 的 100/86/72%** |
| 強調色 | 無 | Action Blue `#0066cc` | **Cobalt `#2148B8`** |
| 分隔線 | — | `#e0e0e0`，獨立的灰 | **主墨版 12%，`#E2E2E0`** |
| 藍底上的字 | — | `#ffffff`，與表面同值 | **`#FAFAF7`，就是紙** |
| 最大字級 | — | 56px | **64px**（為了 5× 級距） |
| 字級名稱 | — | 錯位一格 | **對齊 `tailwind.config.js`** |
| 語氣 | 行銷 | 產品文案 | **不得有促銷語言** |

**沒換的**：SF Pro 堆疊、句首大寫、17px/1.47 內文、負字距、六階圓角、六階斷點、封面區塊、五個 locale。參考站對這些**一句話都沒有**，所以裁決規則的第二句生效，v4 的值原樣留下。

### 本站不採用的項目，以及理由

這張表比前兩版長，因為參考站是印刷品。

| 參考站做法 | 本站決定 | 理由 |
|---|---|---|
| **網點照片**（halftone、risograph grain、cyanotype、photocopy breakup） | **不採用** | 模板裡 `<img>` **實測 0 個**、`<picture>` **0 個**。網點是「把照片轉成墨版」的手段，而本站沒有照片可轉 |
| **一個主體佔版面 45–80%** | **不採用** | 同上。這條規則的主詞是照片或實物，本站沒有那個物件 |
| **標題與主體碰撞**（cross / cover / split around） | **不採用** | 要碰撞得有兩個東西。本站的標題旁邊只有更多文字 |
| **紙張穿過影像**（clipped highlights、knockouts、fade-outs） | **不採用** | 沒有影像就沒有可以被穿過的東西 |
| **一次手繪介入**（圈記、手寫線、套印記號） | **不採用** | 它在參考站的作用是「打斷機械系統」。本站的機械系統是 24 條 CI 規則，不是版面——手繪符號在這裡只會變成一個沒有人管的裝飾類別 |
| **印刷缺陷**（`imperfections.json`：墨色不均、乾邊、套印偏移 1–3mm） | **不採用** | 全部作用在影像版與大字版上。**套印偏移在網頁上不是缺陷，是渲染錯誤** |
| **九種版面家族**（image field、overprint collage⋯） | **不採用** | 九種裡有七種以影像為前提。剩下兩種（ruled information poster、editorial journal）預設多欄，見下一列 |
| **2–3 欄編輯網格** | **不採用**〔推導〕 | 本站是單欄連續正文，橫跨六階斷點。多欄在 600px 以下沒有意義，而那是要支援的最小寬度 |
| **外邊距 5–9% 版面寬** | **不採用**，改記為量得到的事實 | 實測：容器 1152px 時只有 **1280px 視窗**落在區間內（5.0%）。768px 是 0%、1500px 是 11.6%、1920px 是 20.0%。**海報的邊距是版面的性質，網頁的邊距是視窗的性質**，同一條規則跨不過去 |
| **留白 25–55%** | **不採用為規則**，記在「沒有東西檢查的事」 | 沒有任何機械判準量得出網頁的「留白比例」——版面高度由內容決定，分母不存在。**寫成規則會是一條永遠不紅的規則** |
| **載體**（`carriers.json`：海報、zine、唱片套、包裝、服飾） | **不採用** | 七種載體沒有一種是網站 |
| **3:4 直式版面** | **不採用** | 網頁沒有固定比例 |
| **字體是可換的角色卡**（每張圖換一套 display 骨架） | **不採用**，只取其中一張角色卡 | 見「字體」章。一個網站換不了字體——五個 locale 已經在同一套堆疊上，再讓 display 面隨頁面變動會製造 100 條路由的字面不一致 |
| **等寬支援字**（`type_programmatic` 的 support voice） | **不採用**，改取同一張卡的 tabular numerals | `zero mono`（票 #171）是既有規則，而參考站在同一個角色裡就給了替代選項。**這不是繞過參考站，是它自己列了兩個** |
| **禁止 CTA 按鈕、URL、logo** | **不採用** | 海報沒有點擊目標，網站的主要動作就是點擊。**這是印刷／網頁的邊界，不是品味差異** |
| **禁止卡片網格** | **縮小採用**，見「版面」章 | 實測 zh-Hant-TW 一個 locale 有 `grid-cols-2` **9 次**、`grid-cols-3` **7 次**、`grid-cols-5` **2 次**。全面禁止要改 18 處而且沒有替代方案；改成「網格不得用來做等權重的卡牆」 |
| **語氣：禁止促銷語言、口號、宣言** | **採用** | 這一條印刷／網頁都成立，而且本站是技術指南站，本來就不該有促銷語言 |

⚠️ **參考站最核心的主張，第三次從第一天就是空的。** v3 的航太站核心是攝影，本站 0 張；v4 的 Apple 站核心是攝影加產品渲染圖，本站 0 張；v5 的 mono-color 核心是**把照片轉成墨版的網點**，本站還是 0 張。**三次選了三個以影像為核心的參考站，三次都不放圖。** 這不是這一版的失誤，是一個重複三次的挑選模式，寫在這裡是為了讓下一次挑參考站的人先看到它。

**這一版跟前兩次的差別是：這次拿到了一個不依賴影像的東西。** 墨版模型——「最多兩塊版，其餘都是密度階」——是顏色的**組織方式**，不是影像處理。本站先前有五個彼此獨立的顏色 token，沒有任何東西說得出「可以有第六個嗎」。現在說得出：不行，除非它是那兩塊版之一的密度階。**這是三次品牌重置裡第一次拿回一條新的可檢查規則，而不只是換一組值。**

---

## 抽得出來的部分

上一張表列的是丟掉的東西，而**丟掉的理由幾乎全是同一句：本站沒有那個物件**。那句話只在頁面上成立。

### 抽取判準

一條印刷規則要能搬到本站，必須通過三關。三關都過才寫成規則，過兩關的寫成注意事項，過一關的留在不採用表。

| 關 | 問題 |
|---|---|
| ① 有對象嗎 | 這條規則管的東西，本站有沒有？ |
| ② 分母存在嗎 | 它是比例規則的話，分母在網頁上是不是常數？ |
| ③ 量得到嗎 | 有沒有機械判準？沒有的話它是注意事項，不是規則 |

**「留白 25–55%」在頁面上第②關就死了**（版面高度由內容決定，分母不存在），**但它在 OG 卡上三關全過**——卡是 1200×630，分母是常數。**同一條規則，換一個載體就活了。**

### 載體：本站有三種，不是一種

參考站的 `carriers.json` 列七種載體，本站對照下來一種都不是——但**「先宣告載體再套規則」這件事本身抽得出來**，而本站有三種：

| 載體 | 數量 | 尺寸 | 印刷規則適用度 |
|---|---|---|---|
| **頁面** | 100 條路由 | 隨視窗 | 低。比例規則的分母不存在 |
| **OG 分享卡** | **100 張** | **1200×630，全部** | **高。這是本站唯一的印刷品** |
| **圖示與 logo** | 8 個檔 | 16–512px | 中。單色，但沒有排版 |

⚠️ **v4 沒有這一格，所以 100 張卡從來沒有被任何設計規則管過。** 它們只在「衍生資產」那一列出現一次，內容是「會不會印錯字」——那是建置問題不是設計問題。

### OG 卡：本站唯一的印刷品

固定尺寸、一次算繪、之後不再重排——**它就是海報**。所以參考站那些在頁面上第②關會死的比例規則，在這裡全部活著。實測 100 張：

| 參考站規則 | 要求 | 實測 | |
|---|---|---|---|
| 外邊距佔版寬 | 5–9% | **左右都是 80px ＝ 6.67%，100 張完全一致**；上 78px、下 77–81px | ✅ |
| 主體佔版面 | 45–80% | **墨跡外框 64.8%–65.3%**，中位數 65.1% | ✅ |
| 級距（最大字÷微字） | 5–12× | **80/17 ＝ 4.71×**；長標題分支 **62/17 ＝ 3.65×** | ❌ |
| 禁 CTA、URL、QR | 不得出現 | 卡上 0 個 | ✅ |
| 細線由墨版畫 | one-ink rules | `.rule` 1px，走 `--line-rgb` | ✅ |
| 版面家族 | 選一種 | 標題主導 → `type-led declaration` | ✅ |

**四項符合、一項不符合、一項是它本來就在做的。** 這 100 張卡在沒有人拿印刷規範看過它們的情況下，已經照著印刷規範做了大半——因為 `build-og.js` 是照本站的 token 寫的，而本站的 token 這一版換成了印刷模型。

⚠️ **這裡量錯過一次，寫下來因為那個錯很好犯。** 第一次量「留白」用的是**深色像素比例**，得到 86.0%–97.1%（中位數 93.2%），據此得出「100 張全部太空、0/100 通過」的結論。**那個指標是錯的**：文字大部分是字腔與行間空隙，一張純文字的卡不管字放多大，深色像素都到不了 45%。參考站說的 45–80% 是**主體佔的版面範圍**，也就是墨跡的外接矩形，不是墨點數量。換成外接矩形重量，答案從 0/100 變成 **100/100**。**兩個數字都是「量出來的」，而其中一個是錯的——量對東西跟量這件事本身一樣重要。**

#### 唯一沒過的那一項

級距。而它壞在一個**只有 OG 卡有、頁面沒有**的地方：標題字級是分支的。

`scripts/assets/build-og.js:106`：`font-size: ${item.title.length > 34 ? 62 : 80}px`

實測 100 張裡 **59 張走 62px 分支**，而分支比例隨語言差很多：

| locale | 80px | 62px |
|---|---|---|
| `en-US` | 5 | **15** |
| `ko-KR` | 7 | 13 |
| `ja-JP` | 8 | 12 |
| `zh-Hans-CN` | 10 | 10 |
| `zh-Hant-TW` | 11 | 9 |

**所以同一頁的卡，字級隨語言不同。** 那不是設計決定，是標題字數的副作用——標題含 `| TauX` 後綴，長度中位數 36、最長 85（`Post-quantum cryptography migration: five steps and the clocks already running | TauX`），而門檻是 34。

修法算過兩條：

| 做法 | 短標題 | 長標題 | 代價 |
|---|---|---|---|
| 標題升到 ≥85px、砍掉分支 | ✅ 5.0× | ❌ 排不下 | 85px 下 85 字的標題約 5 行，扣掉上下邊距只剩 473px，還要放 kicker、mark、細線與描述 |
| **kicker 17 → 12px** | **✅ 6.67×** | **✅ 5.17×** | 卡在動態消息裡會被縮到約 500px 寬，12px 實際只有約 5px |

**取第二條。** 除了它是唯一兩個分支都修好的做法之外，還有一個理由：**卡上的 kicker 就是頁面上的 `eyebrow`**——同樣大寫、同樣 `0.09em` 字距、同樣的角色，只是字級不同（17 vs 12）與字重不同（400 vs 600）。改成 12px/600 之後它不再是一個獨立的字級，而是頁面刻度上已經有的那一階。

⚠️ **代價是實的，不要當作沒有：長標題分支修好之後是 5.17×，離下限只有 0.17。** 標題再長一點、或 kicker 再大一點，它就掉出去。**這一階沒有餘裕，而頁面那一階（5.33×）有。**

⚠️ **卡上有四個字級**（mark 26、kicker 17、desc 23、title 62–80），而參考站要的是**一個主要 display 聲音＋一個功能性支援聲音**。26 與 23 擠在中間帶，是不是同一個聲音沒有被決定過。**這一格今天是開的**，沒有寫成規則因為還沒想清楚合併哪兩個。

### 頁面側抽得出來的四條

頁面過不了比例規則，但過得了這四條——它們管的是**關係**不是比例，所以沒有分母問題。

1. **細線由墨版畫，不是獨立的灰。** 底線、邊框、分隔線、`hr` 共用一個權重一塊版。本站已經是（`hairline`），v5 只是把它的值改成算出來的。
2. **挖空看到的是紙。** 藍底上的字色不是「白色」，是「表面」。`on-primary` ＝ `canvas`（決策 #80）。
3. **深淺不是新顏色，是同一塊版的密度階。** 這條在頁面上比在印刷上更有用——CSS 讓人可以無成本地生出第六個灰。
4. **顯示文案 2–8 個詞。** 參考站對 display copy 的長度限制，在頁面上對應 `<h1>` 的領銜句與 OG 卡的標題。⚠️ **今天沒有量過頁面的 `<h1>` 詞數**，只量了卡的標題字數（中位數 36 字元）。寫在這裡是待辦，不是結論。

### 印刷規範沒有、網頁一定會遇到的

**這一節是這一版新加的，因為參考站在這些地方一個字都沒有——印刷品不會遇到它們。** 全部實測過。

| # | 事情 | 現況 | 為什麼要寫下來 |
|---|---|---|---|
| 1 | **深色模式** | `prefers-color-scheme` 全站 **0 個** | 墨版模型的答案是**紙就是紙，不反轉**——反轉會讓 `#FAFAF7` 變成一個沒有名字的深色表面。⚠️ 但 `static/taux-logo-light.png` 與 `taux-logo-dark.png` **兩個檔案都在，而 `templates/`、`src/`、`generator/` 裡 0 個使用者**。兩個 logo 變體暗示兩個表面，而本站只有一個 |
| 2 | **`forced-colors`（Windows 高對比）** | 全站 **0 個** | 作業系統會**直接換掉墨版的顏色**。**這是兩塊版的模型唯一會被外力整個推翻的地方**，而本站今天對它沒有任何宣告 |
| 3 | **列印樣式 `@media print`** | 全站 **0 個** | **這一格最諷刺**：參考站是印刷規範，而本站按 Cmd+P 印出來的東西不受這份文件管。**這是唯一一個「參考站的主場」，而本站在那裡是空的** |
| 4 | **`::selection` / `caret-color` / `accent-color`** | 全站 **0 個** | **沒有宣告不等於沒有顏色**，是用作業系統的藍。規則 25 讀樣式表，讀不到沒有宣告的東西——**三個逃出兩塊版的顏色，而閘門看不見它們** |
| 5 | **`<meta name="theme-color">`** | 全站 **0 個** | 手機瀏覽器的網址列會用它上色。同上：那是第四個逃出去的表面 |
| 6 | **捲軸拇指用 alpha 到達密度階** | `::-webkit-scrollbar-thumb:hover` 是 `rgb(var(--ink-rgb) / 0.35)` | ✅ **已決：alpha 算合法密度階**（決策 #105）。這一格現在由規則 32 `density scale` 管——`0.35` 在刻度上，所以它是綠的 |
| 7 | **`prefers-contrast: more` 的髮絲線** | 提到 `138 138 138` | **墨版模型第一次回答了一個既有的值**：`#8A8A8A` 最接近 Charcoal 的 **56% 覆蓋**（`#898B8D`）。改成 56% 之後它不再是挑出來的灰 |
| 8 | **廣色域（P3）** | 沒有處理 | `#2148B8` 在 P3 螢幕上比 sRGB 飽和。**對比是算在 sRGB 上的**，使用者看到的不是那個數字。沒有東西量得出來 |
| 9 | **OG 卡被合成到純白上** | 換表面之後才會有 | 部分社群客戶端把卡貼在 `#ffffff` 卡片上。`#FAFAF7` 在純白旁邊讀成**髒掉的白**，不是紙。⚠️ **這是 v4 沒有的新問題**，因為 v4 的卡底就是 `#ffffff` |
| 10 | **卡的字面取決於建置機器** | 既有問題 | 字體不自架之後，100 張 PNG 的字形取決於跑建置的機器裝了什麼。**印錯的是圖片，改不了也看不出來** |

⚠️ **第 1–5 項有一個共同形狀：它們都是「沒有宣告」而不是「宣告錯了」。** 規則 25 `two plates` 掃樣式表裡出現過的顏色，**掃不到應該出現而沒有出現的**。一條只看得見既有宣告的規則，擋不住從瀏覽器預設漏進來的第三個顏色——所以**規則 31** 是清單式的（列出必須宣告的位置），不是掃描式的。**這是本檔第一次寫一條「檢查缺席」的規則。** ⚠️ 這一句原本寫「規則 32」，而 32 是 `density scale`——**編號寫錯了一格**，同一份文件在同一章裡。

---

## 遷移狀態

這份文件描述的是**目標**，不是現況。v5 今天**只改了這份文件**。

| 章節 | 狀態 | 階段 |
|---|---|---|
| 本文件（來源、不採用表、決策 #74–#88） | ✅ | ⓪ |
| 字級名稱錯位修正（文件側） | ✅ | ⓪ |
| Token — 表面改 `#fafaf7` | ✅ | ① |
| Token — 三階墨色改 Charcoal 密度階（並改名 `--ink-86` / `--ink-72`） | ✅ | ① |
| Token — `primary` 改 Cobalt；`primary-focus` 指向 `primary` | ✅ | ① |
| Token — `hairline` 改主墨版 12%（含 `prefers-contrast` 的 56% 階） | ✅ | ① |
| Token — `on-primary` 寫成 `var(--surface-rgb)` | ✅ | ① |
| 字級 — `display-lg` 56 → 64px（`3.5rem` → `4rem`） | ✅ | ② |
| 字級 — `tailwind.config.js` 與本檔名稱對齊 | ⬜ | ② |
| 規則 25 `two plates`（含 `scripts/plates.js`） | ✅ | ② |
| 規則 26 `accent carries interaction` | ✅ | ② |
| 規則 27 `scale jump` | ✅ | ② |
| 規則 28 `no card wall` | ⬜ | ③ |
| **OG 卡 — kicker 17 → 12px（唯一沒過的印刷規則）** | ✅ | ② |
| **規則 29 `og geometry`（邊距＋主體佔版，今天已綠）** | ⬜ | ② |
| **規則 30 `og scale jump`** | ✅ | ② |
| **`site.webmanifest` 的 `theme_color` / `background_color`（黑底時代殘留）** | ✅ | ① |
| **規則 31 `declared surfaces`（深色／高對比／列印／`::selection`／`theme-color`）** | ⬜ | ③ |
| **focus ring 改靠環寬與 offset 區分（決策 #79 的後半）** | ⬜ | ③ |
| **規則 32 `density scale`** | ✅ | ② |
| 衍生資產 — 100 張分享卡重生 | ✅（v5 首次，因 kicker 改動） | ② |

✅ **階段 ① 已落地，一次改完沒有拆。** 表面、三階墨色、`hairline`、`on-primary` 的值互相定義——`ink-72` 之所以是 `#696B6F`，是因為它是 Charcoal 在 `#FAFAF7` 上的 72% 覆蓋。先改表面不改墨色會得到一組沒有算過對比的顏色。

**實測結果：`contrast` 走過 101 條路由的 24,003 個文字元素，0 個低於 AA、0 個低於 1.5:1。`geometry` 808 個組合 0 失敗。** ⚠️ 這裡先前寫「24,435 個元素」，那是階段 ① 之前的計數；**這一版寫的是這次跑出來的數字**。

⚠️ **`hairline` 那個洞不存在，而且是查出來的不是猜的。** `scripts/visual/contrast.js:35` 自己列著它看不見的東西：`/404`（不是 `[[page]]`）、**border colours**、pseudo elements、SVG fill/stroke。**1.24:1 的髮絲線不會被當成文字讀**，所以那個顧慮從一開始就沒有對象。

---

## Token

全部定義在 `src/input.css` 的 `:root`。**模板裡不得出現任何 hex 值**（`zero hex` 規則）。

### 墨版模型

這是 v5 唯一的結構性新東西，其餘都是它的推論。

**最多兩塊墨版。** 一塊主墨版（Charcoal）、一塊副墨版（Cobalt）。**紙不算墨版。**

**深淺不是新的顏色，是同一塊版的密度階。** 參考站的原話：darker coverage may appear near-black and sparse halftones may appear pale — these are density changes, not extra inks。所以本站的三階墨色從「三個獨立的灰」改寫成「主墨版的三個覆蓋率」，而分隔線是第四個覆蓋率。

**這條規則的用處不在今天的顏色，在明天的第六個。** v4 的五個 token 每一個都有理由，但沒有東西回答得了「再加一個算不算違規」。現在回答得了：**算，除非它是那兩塊版之一的密度階。** 由規則 25 `two plates` 執行。

### 表面

**單一表面**：`{colors.canvas}` `#FAFAF7`（Neutral White）。不做暗磚、不做第二層表面。

參考站給三種紙：Neutral White `#FAFAF7`、Cool Gray `#E9E9E5`、Pale Beige `#F5F1E8`，並且明文說 Cool Gray 配 architecture、technology、restrained branding——**字面上最貼本站的行業**。本站取 Neutral White，理由是算出來的不是選出來的：

| 紙 | `#707070` 舊次階 | `#30343A` | `#2148B8` |
|---|---|---|---|
| Neutral White `#FAFAF7` | **4.74** ✅ | 11.97 | 7.48 |
| Cool Gray `#E9E9E5` | **4.07** ❌ | 10.28 | 6.43 |
| Pale Beige `#F5F1E8` | **4.39** ❌ | 11.10 | 6.94 |

Cool Gray 會讓最低那一階掉到 4.07，低於 AA 的 4.5。**這與 v4 踩到 `#7a7a7a` 是同一個坑，只是這次在改之前算了。**

⚠️ **上表最後一欄是舊值，留著是為了說明「為什麼不選 Cool Gray」，不是新的次階。** 新的次階見下一段，它是推導出來的，不是沿用的。

⚠️ **`#FAFAF7` 不是白色。** 它與 `#ffffff` 差 5/5/8。參考站要求 keep the paper visible、never tint the whole page into a digital monochrome wash——紙要讀得出來是紙。這個差是刻意的，`surface is painted once`（規則 23）守著它只被畫一次。

### 主墨版 Charcoal

`#30343A`。參考站列它給 architecture、photography、research、restrained publications——本站是技術指南站，落在 research 那一格。

三階密度，全部算過對比（紙 `#FAFAF7`）：

| Token | 覆蓋率 | 值 | 對比 | 用途 |
|---|---|---|---|---|
| `{colors.ink}` | 100% | `#30343A` | **11.97:1** | 標題與內文 |
| `{colors.ink-86}` | 86% | `#4C5054` | **7.78:1** | 次要文字 |
| `{colors.ink-72}` | 72% | `#696B6F` | **5.11:1** | 圖說、meta |

**名字換成覆蓋率，而不是沿用 `-80` / `-48`。** 舊名字是不透明度，新值是覆蓋率，兩套數字不同義；沿用會產生一個 `ink-80` 卻是 86% 的 token。⚠️ **代價：這個改名會動到 `input.css` 與模板裡每一個舊名字的使用者，而 `check:classes` 抓得到未知類別、抓不到改對名字但用錯階的地方。**

最低階 **5.11:1 比 v4 的 4.95 高**，而且它不是挑出來的——64% 是 4.06（不到 AA），72% 是第一個過的整數階。**這一階是唯一沒得選的，其餘兩階可以往上調。**

### 副墨版 Cobalt

`{colors.primary}` `#2148B8`（**7.48:1**）。參考站列 Cobalt 給 technology、knowledge、cities——與本站對得上，而且它是參考站自己的 one-ink 預設。

⚠️ **這一段一度寫「承載所有互動：連結、CTA、focus ring」，而三項裡有兩項是假的**（決策 #109）。實測全站 Cobalt 只有**兩個**用途：

| 文件曾經說 | 實測 |
|---|---|
| 連結 | ❌ **`a` 的顏色宣告 0 個**。連結繼承 `{colors.ink}`，靠底線標記 |
| CTA | ✅ `.btn` 的底色，`src/input.css:412` |
| focus ring | ❌ `src/input.css:292` 是 `outline: 2px solid var(--ink)` |

加上 15 個 `<button>` 上的 `text-primary`，就是全部。**這句話從 v4 寫到 v5，跨兩次品牌重置，沒有任何東西讀它**——直到規則 26 把它變成閘門的那一天，它才第一次被對照。

**副墨版只有一個角色，而角色要在排版之前指定。** 參考站的原話：give the dominant and accent plates separate content roles before composing; never use the second ink merely to decorate。本站的角色是**互動**——藍色不是裝飾。

⚠️ **但它不是「互動的唯一記號」，而那句話寫了兩版。** 實測 `zh-Hant-TW` 的 160 個 `<a>`：**112 個沒有任何標記 class**，36 個 `underline`，11 個 `hover:underline`，1 個 `underline underline-offset-2`（元件規則另外用 `@apply underline underline-offset-4` 補一部分）。**連結從來沒有藍過。**

**標記連結的是底線，不是顏色，而那比較好**：WCAG 1.4.1 要求資訊不得只靠顏色傳達，所以一個只用藍色標記連結的站本來就不合格。**程式這邊做得比文件說的對**，所以改的是文件（決策 #109）。

規則 26 `accent carries interaction` 守的是**反方向**：Cobalt 不得出現在不能點的東西上。它不要求所有互動都是 Cobalt——那會把底線標記的 112 個連結全部判紅。**這是本站第四次面對「要不要第二個強調色」，而這次終於有一條規則而不只是一句話**：規則 26 `accent carries interaction`。

⚠️ **`{colors.primary-focus}` `#0071e3` 移除。** 在兩塊版的上限下，第三個藍是第三塊版。

⚠️ **這一段原本接著寫「focus ring 改用 `{colors.primary}`，代價是 focus ring 與連結同色，鍵盤使用者要靠形狀分辨聚焦」，並標為「v5 讓可用性變差的唯一一處」。那個代價是虛構的**（決策 #109）：focus ring 從來不是 Cobalt，`src/input.css:292` 寫的是 `outline: 2px solid var(--ink)`，**在紙上 11.97:1，比 Cobalt 的 7.48 更高**。移除 `primary-focus` 沒有改變任何人看到的東西。

⚠️ **而 `--primary-focus` 實測是零使用者**：只有它自己的宣告，加上 `tailwind.config.js` 把它暴露成一個沒有人寫過的 Tailwind 顏色。**一個 token、一個顏色出口、零個呼叫點，而文件為它寫了一整段代價分析。**

**寫下一個沒發生的代價，比不寫代價更糟**：它讓下一個人以為那個權衡做過了。

`{colors.on-primary}` `#FAFAF7`——**從墨版裡挖空，看到的是紙**。這個值等於 `{colors.canvas}` 是推導的結果，不是巧合。⚠️ v4 說這兩個 token 要分開命名，理由是「表面變了」與「藍底上的字變了」不該是同一次改動。**那個理由現在反過來了**：在墨版模型下，紙變了，挖空看到的東西**就是**跟著變。名字保留（改起來不划算），但它現在是 `canvas` 的別名而不是獨立值——**任何時候這兩個值不相等，那是 bug 不是設計**。

### 髮絲線

`{colors.hairline}` `#E2E2E0`，1px——**主墨版 12% 覆蓋**。唯一的分隔與邊框權重。

參考站說 thin one-ink rules，線是墨版畫的。v4 的 `#e0e0e0` 是獨立的灰，與任何墨色沒有關係；新值差它 2/2/0，但**來源不同**：一個是挑的，一個是算的。

對紙的對比 **1.24:1**。⚠️ **這個值不受 AA 管，因為它不是文字**——但 `contrast` 閘門如果把帶 `border` 的元素當成文字讀，它會紅。改的時候要確認閘門對非文字元素的處理，這件事**沒有被驗證過**。

### 圓角

〔推導〕**參考站一個字都沒提圓角**——印刷品沒有這個屬性。裁決規則第二句生效，v4 的六階原樣保留：`none` 0 · `xs` 5px · `sm` 8px · `md` 11px · `lg` 18px · `full` 9999px。

⚠️ **六階裡只有 `full` 有使用者，實測 zh-Hant-TW 一個 locale 55 次、五個 locale 合計 275 次。** v4 寫的是 86 次，那個數字現在對不上——它是加 locale 之前的計數，而**加 locale 沒有回頭改這個數字**。這與規則計數停在 21、CI 註解停在 15 是同一類漂移，第三次。

守著它的是 `radius scale`（規則 4）：圓角必須是這六個值之一，不得出現第七個。

---

## 字體

### SF Pro，且接受平台差異

display 走 **SF Pro Display**，內文走 **SF Pro Text**，兩者都不自架——它們只存在於 Apple 平台，其餘落到 `system-ui`（Windows 的 Segoe UI、Android 的 Roboto）。**代價：品牌字面在 Windows 上不是 SF Pro。**

〔推導〕**參考站對此無話可說**，因為它每張圖都可以換字體。網站不行，見下一段。

### 排版角色只取一張，不取整副牌

參考站的 `typography.json` 給七張角色卡，並且明文說 typography is a responsive cast, not a fixed house font——**每張圖依主題換 display 骨架**。

**本站取 `type_programmatic` 一張，其餘六張不採用。**

那張卡的原文：medium grotesk or engineered modular sans；support 是 tabular numerals / mono / narrow information face；行為是 dates and numerals may become the anchor、ruled clusters、unequal information blocks、**bilingual-safe spacing**；use_for 是 festival program、calendar、**research**、architecture、multi-event information。

選它的理由有三個，都是本站的事實不是偏好：15 頁長文指南落在 research；五個 locale 的雙語混排就是 bilingual-safe spacing 要解的問題；以及 SF Pro 本來就是 engineered modular sans。

⚠️ **「一張卡」這件事本身就是不採用參考站。** 它要的是一套會隨內容變動的字體卡司，而本站有 100 條路由共用一份樣式表——**換 display 面只有兩種實作方式：自架多套字體（決策 #4 已否決），或依頁面切系統字（會讓同一個標題在 100 條路由上長得不一樣）**。兩個都比「少一點變化」貴。

**support voice 取 tabular numerals，不取 mono。** 同一張卡列了三個選項，而 `zero mono`（規則 13）已經在守著。這不是繞過參考站——是它自己列了三個，本站挑了與現行規則相容的那個。

### 系統中文，而且不只一種

中文一律落到系統字。**不引入 CJK webfont**（決策 #4）。SF Pro 不含 CJK，所以兩條堆疊都要掛著系統 CJK 面。

**每個 locale 一組 CJK 面**，由 `html[lang]` 換 token：

| locale | macOS | Windows | 備援 |
|---|---|---|---|
| `zh-Hant-TW` | PingFang TC | Microsoft JhengHei | Noto Sans TC |
| `zh-Hans-CN` | PingFang SC | Microsoft YaHei | Noto Sans SC |
| `ja-JP` | Hiragino Sans | Yu Gothic UI | Noto Sans JP |
| `ko-KR` | Apple SD Gothic Neo | Malgun Gothic | Noto Sans KR |
| `en-US` | 繁中面墊底 | 同左 | 同左 |

`en-US` 仍掛 CJK 面，因為英文頁會出現中文專有名詞（公司登記名、地址），而**沒有面就會落到系統預設，那在 Windows 上是明體**——決策 #40 記過同一件事。

**沒有東西量得出這台機器看到的是不是對的那一面。** 五個 locale 都是。

### 字重階梯

**300 / 400 / 600 / 700，刻意沒有 500。** 內文永遠 400，行內強調 600，display 600。由 `weight ladder`（規則 21）守著。

---

## 字級與字距刻度

⚠️ **這張表的名字是 `tailwind.config.js` 宣告的名字**，不是 v4 那組錯位一格的名字。對照見決策 #87。

| Token | 字級 | 字重 | 行高 | 字距 |
|---|---|---|---|---|
| `display-lg` | **64px** ✅ | 600 | 1.07 | −0.005em |
| `display-md` | 40px | 600 | 1.1 | 0 |
| `display-sm` | 34px | 600 | 1.18 | −0.011em |
| `display-sub` | 24px | 400 | 1.4 | 0 |
| `lead` | 21px | 400 | 1.48 | 0.011em |
| `body` | 17px | 400 | **1.47** | −0.022em |
| `body-strong` | 17px | 600 | 1.47 | −0.022em |
| `caption` | 14px | 400 | 1.43 | 0 |
| `eyebrow` | 12px | 600 | 2.0 | 0.09em |

### 級距：最大字必須是最小字的 5–12 倍

參考站唯一一條直接管字級的規則：use one dramatic scale jump: largest text is 5-12 times the microcopy size。

**這是本站唯一沒過的既有值。** 算出來：

| `display-lg` | ÷ `eyebrow` 12px | 參考站 5–12× |
|---|---|---|
| 56px（v4 現值） | **4.67×** | ❌ 不到 5 |
| 60px | 5.00× | 剛好在界上 |
| **64px** | **5.33×** | ✅ |
| 72px | 6.00× | ✅ |

取 **64px**（4rem）。60 剛好壓在 5.00，任何一次微調都會掉出去；64 有餘裕而且是整數 rem。

⚠️ **分母取 `eyebrow` 12px 不是 `caption` 14px。** 參考站說的是 microcopy，而本站最小的字是 eyebrow。用 14px 當分母的話 64/14 = 4.57，一樣不過——**兩個分母都算過，寫下來是為了讓下一個人不用重算。**

⚠️ **這一階往上動，`.display-lead` 的手機值沒有跟著動。** `src/input.css` 寫的是 `text-4xl tablet:text-display-lg`，`text-4xl` 是 36px——**手機上的級距是 36/12 = 3.0×，而規則 27 如果照抄「最大 ÷ 最小」會在手機斷點紅掉。** 規則要寫成「刻度上的最大值」而不是「某個斷點上量到的最大值」，否則它管的是斷點不是刻度。這一句是寫規則的人會踩的第一個坑。

### 行高

**內文行高不得低於 1.47。** 編輯性的行距是這套語彙的一部分，三個參考站在這一點上難得一致。

---

## 版面

### 章節區塊

每個 `<h2>` 包在一個 `[data-cover]` 區塊裡，只有 eyebrow 與標題，**不放 CTA**。**高度是內容高度，不是一屏。**

〔推導〕參考站的對應概念是 one focal event per page——每一版只有一個焦點事件。**本站的一條路由不是一版**，它是連續捲動的長文；把「一頁一個焦點」硬套過來只有兩種讀法：整條路由只准一個 `<h2>`（荒謬），或每個章節區塊算一版（那就是本站已經在做的事）。**取後者，並且明文寫下這是推導不是採用。**

### 正文連續

封面區塊之後的正文正常流動，不切屏。全站 39,456 字。

### 閱讀欄與行長

文字落在置中的閱讀欄內。⚠️ **本站有兩個容器寬度同時在用**，實測 zh-Hant-TW：`max-w-4xl`（56rem = 896px）**23 次**、`max-w-6xl`（72rem = 1152px）**8 次**。`<main>` 自己不設寬度（`class="pt-16 min-h-screen relative overflow-x-clip"`）。

v4 寫「實測寬度 1152px（`max-w-6xl`）」，那句話對 8 個容器成立、對 23 個不成立。**它不是錯的，是只講了少數那一半。** 這一格沒有改成單一值，因為改容器是版面改動不是文件改動——寫在這裡是為了讓 `geometry` 的 `measure` 探針的讀者知道它在量的是兩種欄寬的混合。

**一行的寬度上限 68ch，五個 locale 相同**，由 `:where(main p, main li) { max-width: 68ch }` 無條件執行（`src/input.css:159`），`tailwind.config.js:124` 的 `prose` 同值。實測 101 條路由、2818 個元素，**沒有一個** cap 更寬——所以這個天花板是**樣式表常數，不是任何書寫系統的性質**（決策 #73）。

### 外邊距——量得到，但管不住

參考站要求 generous outer margins: 5%-9% of page width。**本站不採用為規則**，因為它在網頁上不是常數：

| 視窗 | 邊距（容器 1152px） | 佔比 | 5–9%？ |
|---|---|---|---|
| 600px | 0px | 0.0% | ❌ |
| 768px | 0px | 0.0% | ❌ |
| 961px | 0px | 0.0% | ❌ |
| 1280px | 64px | **5.0%** | ✅ |
| 1500px | 174px | 11.6% | ❌ |
| 1920px | 384px | 20.0% | ❌ |

**六個寬度裡只有一個在區間內。** 海報的邊距是版面的性質（分母固定），網頁的邊距是視窗的性質（分母由使用者決定）。**這條規則跨不過印刷／網頁的邊界，而不是本站沒做到。**

### 網格不得做成卡牆

參考站的 hard avoid 列了 card grids、UI panels、centered template symmetry、never distribute objects evenly like a template。

**全面禁止不採用**——實測 zh-Hant-TW 有 `grid-cols-2` 9 次、`grid-cols-3` 7 次、`grid-cols-5` 2 次，共 18 處，而其中大多數是表格式的資訊並排，不是卡牆。

**縮小成一條做得到的**：網格可以用來並排資訊，**不得用來排出一面等權重、等大小、每格都有邊框與圓角的卡牆**。由規則 28 `no card wall` 執行——判準是**每一格都同時帶邊框＋圓角＋內距**，那是卡片的形狀。⚠️ **這條規則今天沒有實作，也還沒有量過有幾處會紅。** 它排在階段 ③，因為在量之前寫不出門檻。

### 分隔之後的間距，三階

一條掛在 `<section>` 上的髮絲線是本站的**群組邊界**，而它後面的留白說的是「這兩群離多遠」。

| 情況 | 留白 | 數量 |
|---|---|---|
| section 開頭是 `.cover` | cover 自己的 `padding-block` **96px**，section 不帶 `pt` | 45 |
| 頂層 section，直接接內容 | `pt-16`（**64px**） | 355 |
| **巢狀**子 section | `pt-12`（**48px**） | 20 |

組內間距是 `space-y-6`（24px）與 `gap-4`（16px），所以**組間比組內約 2.7:1**。由 `section gap scale`（規則 24）執行。

### 斷點

〔推導〕**參考站沒有斷點**。六階原樣保留：`< 600` / `mobile 600` / `tablet 768` / `laptop 961` / `desktop 1280` / `wide 1500`。

---

## 元件

〔推導〕**這一整章都是推導。** 參考站的 hard avoid 明文禁止 CTA buttons、URLs、logos——海報沒有點擊目標。本站的主要動作就是點擊，所以 v4 的元件原樣保留，只換顏色來源。

### `button-primary` — 藍色 pill CTA

背景 `{colors.primary}` Cobalt、文字 `{colors.on-primary}`（＝紙）、圓角 `{rounded.full}`、字體 `body-strong`。**主要 CTA 只有這一種。**

### `button-utility` — 緊湊矩形

背景透明、1px `{colors.hairline}` 邊框、文字 `{colors.ink}`、圓角 `{rounded.sm}` 8px。次要動作用。

### `nav`

單層，貼齊頂端，紙底、1px 髮絲線收邊。**不做雙層、不做 frosted `backdrop-filter`。**

### `footer`

紙底、`{colors.ink-86}` 文字、髮絲線分隔。

### 404

單一區塊：句首大寫的拉丁領銜句、中文副標、一個回首頁的藍色 pill。

---

## 動態

〔推導〕**參考站是靜態印刷品，完全沒有動態。** 全章保留 v4。

- **easing 只有一種**（規則 11）。
- **沒有陰影、沒有漸層、沒有模糊。** 這一條在 v5 有了新的理由：參考站的 hard avoid 明文列出 gradients、glossy mockups、3D depth、cinematic lighting、lens blur、hard shadows。**v4 的理由是「沒有影像就沒有陰影」，v5 是參考站直接禁止。** 同一條規則，依據換了，而且變強了。
- **沒有捲動揭示**（規則 2）。
- **按下回饋保留**：每個有 `:hover` 的元件也要有 `:active`，且寫在其後（規則 12）。
- **一個狀態的宣告必須與它覆蓋的值不同**（規則 20）。

---

## 本站推導

### 中文標題

H1 兩行：**領銜句**（拉丁、**句首大寫**、`.display-lead`）與**副標**（中文、`.display-sub`）。

`heading structure`（規則 5）是條件式的：`display-lead` 每個 locale 都要，`display-sub` 只在該 locale 不使用拉丁文字時才要求（`zh-Hant-TW`、`zh-Hans-CN`、`ja-JP`、`ko-KR` 要，`en-US` 不要）。**規則沒有被關掉，是被縮小到它成立的範圍。**

### 語氣

**這是 v5 唯一一條從參考站整條搬過來、而且印刷／網頁都成立的內容規則。**

參考站：terse, observant；human and specific rather than inspirational；quiet confidence, dry wit, or factual clarity；**no sales language, CTA, hype, productivity slogans, or brand manifesto voice**；並且 for factual, civic, scientific, or archival subjects, let clarity override。

本站 15 頁是技術指南，落在 factual／scientific 那一格，所以**取清晰、不取浪漫**。

⚠️ **「不得有促銷語言」沒有東西檢查，而且大概檢查不了。** 寫在這裡是因為它是規則，不是因為它是閘門——見「沒有東西檢查的事」。

### 長文頁

20 條路由裡 15 頁是長文指南。章節區塊 ＋ 正文連續，見版面章。

### 程式碼與機器輸出

沒有等寬字（規則 13 `zero mono`）。`<pre>` 與 `<code>` 走內文堆疊，行內 `code` 配 `bg-ink/5` 薄層作為唯一區別。**`<pre>` 的內容不得依賴欄位對齊**，靠縮排可以。

✅ **`bg-ink/5` 是主墨版的 5% 覆蓋，實測算繪後是 `#F0F0EE`**（先前寫 `#F3F3F1`，那是舊表面 `#ffffff` 上的值，換紙之後沒跟著改）。它是合法的密度階，**在刻度上**，由規則 32 `density scale` 守著。全站 **185 個**使用者，是這把梯子上最多人走的一階。

### shop、暗磚、網點與手繪介入不採用

見「本站不採用的項目」。**寫下來而不是刪掉**，否則下一個讀參考站的人會以為那是待辦。

---

## 檢查

| # | 規則 | 狀態 | 由誰開啟 |
|---|---|---|---|
| 1 | zero literal colour ⚠️ | ✅ | #50（v5 加寬） |
| 2 | no scroll reveal | ✅ | #50 |
| 3 | tracking scale | ✅ | #53 |
| 4 | radius scale | ✅ | #66 |
| 5 | heading structure | ✅ | #55 |
| 6 | anchor integrity | ✅ | #66 |
| 7 | collapsible ships open | ✅ | #130 |
| 8 | nav breakpoints paired | ✅ | #134 |
| 9 | css version is derived | ✅ | PR 145 |
| 10 | invisible is inert | ✅ | issue 143 |
| 11 | easing scale | ✅ | issue 156 |
| 12 | press follows hover | ✅ | issue 156 |
| 13 | zero mono | ✅ | #171 |
| 14 | zero canvas | ✅ | #166 |
| 15 | section cover screens | ✅ | #182 |
| 16 | sentence case display | ✅ | #193 |
| 17 | single stylesheet | ✅ | #189 |
| 18 | single walker | ✅ | #190 |
| 19 | single route table | ✅ | #191 |
| 20 | states differ | ✅ | #192 |
| 21 | weight ladder | ✅ | #194 |
| 22 | locale relative links | ✅ | issue 199 |
| 23 | surface is painted once | ✅ | 格式塔盤點 |
| 24 | section gap scale | ✅ | 格式塔盤點 |
| **25** | **two plates** | ✅ | **v5** |
| **26** | **accent carries interaction** | ✅ | **v5** |
| **27** | **scale jump** | ✅ | **v5** |
| **28** | **no card wall** | ⬜ | **v5** |
| **29** | **og geometry** | ⬜ | **v5** |
| **30** | **og scale jump** | ✅ | **v5** |
| **31** | **declared surfaces** | ⬜ | **v5** |
| **32** | **density scale** | ✅ | **v5** |

**29 條啟用，3 條待實作。** `scripts/check-design.js` 的 `RULES` 陣列今天有 29 個 `name:`，與這張表打勾的 29 列一致——**這是這份文件第一次把兩邊對過**，而先前三次漂移（表停在 21、CI 註解停在 15、圓角計數停在 86）都是因為沒有人對。

⚠️ **第 1 條改名了：`zero hex` → `zero literal colour`。** 它的 summary 一直寫著「colour values belong to the tokens in `src/input.css`, not the templates」，而實作只比對 `#rrggbb`——**`rgb()` / `rgba()` / `hsl()` / `hsla()` 一律看不見**。實測 **25 個字面色**從那個縫隙走過去，全在 `claude-skills-guide.html` 的五份 locale 副本裡。**規則的名字與它的說明差了一整個顏色語法家族**，而說明是對的那一邊，所以加寬實作、改掉名字。

⚠️ **仍然沒有涵蓋，具名寫在這裡而不是留成暗示**：`oklch()`、`lab()`、`color()` 與具名色（`white`、`red`）。**今天各實測 0 個**，所以沒有為不存在的情況長分支——但一個宣稱「沒有字面色」的 summary 會把這條規則原本的錯誤再犯一次，因此 summary 只列它真的讀得到的四個家族。

⚠️ **`zero canvas`（第 14 條）管的是 HTML 的 `<canvas>` 元素，不是本檔的「表面／canvas」。** 它的豁免清單是 `CANVAS_EXEMPT_IDS = new Set(["jailbreakChart"])`。**兩個 canvas 同形不同義**，而 v5 把表面的值從 `#ffffff` 換成 `#FAFAF7` 之後，這個混淆的代價會變高——改的人會去找 `zero canvas` 然後改錯檔案。

### 八條新規則要檢查什麼

**25 `two plates`** — 樣式表算出來的每一個顏色，都必須是（a）表面 `#FAFAF7`、（b）Charcoal `#30343A` 的某個覆蓋率，或（c）Cobalt `#2148B8` 的某個覆蓋率。**三者以外的顏色一律紅。** ⚠️ 未決：`bg-ink/5` 這類 alpha 語法算不算合法的密度階（見「程式碼與機器輸出」）。

**26 `accent carries interaction`** ✅ — Cobalt 只能出現在能點的東西上，**或在能點的東西裡面**。判準是位置不是名單：`<a>`、`<button>`、`<label>`、`<summary>`、`CONTROL_TAGS`、`.btn`、`.tag`，以及它們的後代——`<button>` 裡的 `<span class="text-primary">` 是那顆按鈕的記號的一部分，不是第二次用副墨版。跟規則 23 同一個形狀。

**它守的是單向的。** Cobalt 不得塗在不能點的東西上；它**不**要求所有互動都是 Cobalt——那會把 112 個靠底線標記的連結全部判紅。

⚠️ **這條規則是綠著上線的，所以用注入證明過它會紅**：作者 CSS 的 `.probe-decorative { color: var(--primary) }` → 紅；模板 `<div class="text-primary">` → 紅。**一條沒被看過紅燈的規則，跟一條不可能紅的規則從外面看一樣。**

⚠️ **把這句散文變成閘門的那一刻，才發現它三分之二是假的**（決策 #109）。

**27 `scale jump`** ✅ — 刻度上的最大字級 ÷ 最小字級落在 5–12。落地時先紅（`3.5rem / 0.75rem = 4.67×`），`display-lg` 改 `4rem` 之後綠（**5.33×**）。

⚠️ **讀的是刻度，不是任一斷點量到的值。** `.display-lead` 是 `text-4xl tablet:text-display-lg`，而 `text-4xl` 是 Tailwind 自己的 `2.25rem`——**手機上實際畫出來的最大字是 36px，3.0×**。照斷點量會讓這條規則報告的是斷點而不是語彙，並且為一個刻度從未做過的決定在手機上紅掉。

⚠️ **比值不換算 px，因為每一階都用 rem 宣告。** `3.5rem / 0.75rem` 與根字級無關——而本站的根字級**曾經被膨脹到 17px**（見「閱讀欄與行長」那一段的代價），一條先換算成 px 的規則會跟著那次膨脹靜靜地移動。

⚠️ **最小的那一階有兩個名字**：`eyebrow` 與 `nav` 都是 `0.75rem`。規則取的是最小值不是某個名字，所以兩個都動了才會改變分母。

**最大字級沒有取 `3.75rem`（剛好 5.00×）**——那壓在下限上，任何一次微調都會掉出去。`4rem` 是 5.33×，有餘裕而且是整數 rem。

**28 `no card wall`** — 網格的每一格不得同時帶邊框＋圓角＋內距。⚠️ **門檻寫不出來，因為還沒量過。** 排階段 ③。

**29 `og geometry`** — 100 張 OG 卡：外邊距佔版寬 5–9%，墨跡外框佔版面 45–80%。**今天全綠**（6.67%、64.8–65.3%），寫成規則是為了鎖住它——`build-og.js` 的 `padding` 現在沒有東西守著。⚠️ **量的是墨跡的外接矩形，不是深色像素比例。** 用後者會得到 0/100 的假紅，理由見「OG 卡」章。

**30 `og scale jump`** ✅ — 卡上最大字級 ÷ 最小字級落在 5–12，**兩個標題分支各斷言一次**。落地時先紅（`80/17 = 4.71×`、`62/17 = 3.65×`），kicker 17 → 12px 之後綠（`6.67×` 與 `5.17×`）。

**它讀的是具名匯出，不是原始碼。** `build-og.js` 現在匯出 `TYPE_SCALE`，CSS 由它產生，規則 `require` 它。⚠️ **regex 版本抓不到標題**——標題寫成 `${cond ? long : short}px`，而且模板字串一重排就會靜默轉綠。⚠️ **同一次改動把 `main()` 包進 `require.main === module`**：先前它無條件執行，`require` 這個檔會開 Chromium 重寫 100 張 PNG，當成 lint 的副作用。

⚠️ **這條與規則 27 是兩條，不是一條**：一條讀 `tailwind.config.js` 的刻度，一條讀 `build-og.js` 的 `TYPE_SCALE`，**兩份來源仍然沒有東西比對**。卡在 4.71× 待著的時候，頁面那把梯子正被推到 5.33×。

**31 `declared surfaces`** — 這五個位置**必須有宣告**，而且值必須是表面或某塊墨版的密度階：`prefers-color-scheme`、`forced-colors`、`@media print`、`::selection`（連同 `caret-color`、`accent-color`）、`<meta name="theme-color">`。**五個今天都是 0 個。** ⚠️ **這是本檔第一條「檢查缺席」的規則**——其餘 31 條都在檢查寫出來的東西有沒有違規，這一條檢查該寫的有沒有寫。掃描式的規則抓不到它，所以判準是清單不是模式。

**32 `density scale`** — alpha 值必須落在一組列舉的刻度上，形狀與 `tracking scale`（規則 3）、`radius scale`（規則 4）相同，是第三個同形狀的 Set。

⚠️ **規則 25 的未決問題已經有答案：alpha 算合法密度階。** 三個依據：(a) 參考站自己就是這樣定義的——「darker coverage may appear near-black and sparse halftones may appear pale — these are density changes, not extra inks」，印刷的密度階就是降低覆蓋率；(b) **`--ink-rgb / 0.12` 算出來是 `#E2E2E0`，與 `hairline` token 完全相同**——同一階兩條路到達，禁止 alpha 等於禁止一條已經走通的路；(c) 禁止的代價是 186 個呼叫點，而且禁掉之後仍然要給它一個名字。

**所以規則 25 檢查底色、規則 32 檢查 alpha。** 25 問「這是哪一塊版」，32 問「這一階在刻度上嗎」。

⚠️ **今天有 7 個沒人宣告的密度階**：`0.05`、`0.06`、`0.12`、`0.2`、`0.35`、`0.8`、`0.9`（`src/input.css` 的 14/276/428/464/736/741 行，加上 Tailwind 的 `bg-ink/5`）。**`0.05` 與 `0.06` 在紙上是 `#F0F0EE` 對 `#EEEEEC`，差 2 級，肉眼分不出**——那是決策 #52 記過的「沒人管的刻度長出第四階」，同一件事。刻度必須包含 `0.12`，因為它就是髮絲線。

⚠️ **這一段先前寫「今天實測 2 個實例」，那個數字是錯的。** 真正的數字是 `bg-ink/5` **185 個**、`bg-surface/90` **1 個**、`src/input.css` **6 處** `rgb(var(--x) / N)`，另有 **25 個字面 `rgba()`**（見規則 1）。**寫 2 的時候數的是「我當時想得到的例子」，不是量出來的東西**——本檔開場反對的就是這個。

### 沒有東西檢查的事

- **語氣。** 「不得有促銷語言、口號、宣言」是 v5 從參考站搬過來最完整的一條內容規則，而**沒有任何機械判準抓得到它**。`check:md` 抓格式，抓不到一句話是不是在賣東西。五個 locale 乘以 100 條路由，這一條靠人。
- **留白 25–55%。** 參考站的核心比例之一，網頁上分母不存在（版面高度由內容決定）。**寫成規則會是一條永遠不紅的規則**，所以不寫。
- **紙讀不讀得出來是紙。** `#FAFAF7` 與 `#ffffff` 差 5/5/8，在校正過的螢幕上分得出來，在沒校正的螢幕上分不出來。**沒有東西量得出使用者看到的是哪一個。**
- **哪一面 CJK 字被實際選中。** 五個 locale 都是，v4 記過同一條。
- **五份內容講的是不是同一件事。** 結構對齊守得住（每條路由在每個 locale 都存在、每個 `id` 錨點都在、字數不得低於基準的 60%），**語意對不上守不住**。
- **分享卡的字面。** **100 張 PNG** 是 commit 進 repo 的位元組，而字體不自架之後，它們的字面取決於跑建置的那台機器裝了什麼。**印錯的是圖片，改不了也看不出來。** ⚠️ **v5 會讓這 100 張全部要重生**——底色從 `#ffffff` 變 `#FAFAF7`、墨色從 `#1d1d1f` 變 `#30343A`、藍色從 `#0066cc` 變 `#2148B8`。它排在階段 ③。
- **`hairline` 會不會被 `contrast` 閘門當成文字讀。** 1.24:1 遠低於任何門檻，如果閘門把帶 `border` 的元素當文字，階段 ① 會紅。**這件事沒有被驗證過，改之前先驗。**

---

## 詞彙表

| 詞 | 定義 |
|---|---|
| **紙 / 表面 / substrate** | 單一背景色，暖白 `#FAFAF7`。**不計為墨版** |
| **墨版 / plate** | 一種印刷色。本站上限**兩塊**：主墨版 Charcoal、副墨版 Cobalt |
| **密度階 / density step** | 同一塊墨版的不同覆蓋率。**深淺不是新的顏色** |
| **主墨版 / dominant plate** | Charcoal `#30343A`，三階：100 / 86 / 72% |
| **副墨版 / accent plate** | Cobalt `#2148B8`，承載互動，沒有第二個用途 |
| **挖空 / knockout** | 從墨版裡挖掉，露出紙。所以 `on-primary` ＝ `canvas` |
| **髮絲線 / hairline** | 主墨版 12%，1px。唯一的分隔與邊框權重 |
| **eyebrow** | 標題上方的小型全大寫標籤。**也是級距規則的分母** |
| **領銜句 / display-lead** | H1 的拉丁首行，**句首大寫** |
| **副標 / display-sub** | H1 的中文次行 |
| **封面區塊 / cover block** | 章節標題專用的區塊，只有 eyebrow 與標題，沒有 CTA。內容高度，不是一屏 |
| **`data-cover="sr"`** | 封面區塊的唯一變體，⚠️ **全站 0 個**。變體保留在這裡是因為規則仍然接受它 |
| **樣式表 / stylesheet** | 本站出貨的所有作者寫的 CSS，不論來自 `input.css`、模板的 `<style>` 或 `style=""` |
| **走訪 / walk** | 一次瀏覽器工作階段走過每條路由 × 每個視窗 |
| **探針 / probe** | 掛在走訪上的一筆資料：`inPage` 或 `onPage` |
| **路由表 / route table** | `site.toml` 的唯一 JS 讀者是 `routes.js`，匯出 PAGES / DOCUMENTS / ROUTES |
| **狀態 / state** | `:hover`、`:active`、`:focus-visible`、`[aria-*]`。宣告必須與它覆蓋的值不同 |
| **〔推導〕** | 標在段落開頭，表示參考站對這件事沒有規定，由本站補位。**權威等級低於參考站直接給的** |
| **卡牆 / card wall** | 每一格同時帶邊框＋圓角＋內距的等權重網格。參考站的 hard avoid，本站縮小採用 |
| **載體 / carrier** | 設計被印在什麼上面。本站有三種：頁面、OG 卡、圖示。**規則先綁載體再綁元素**——同一條規則在不同載體上死活不同 |
| **抽取判準** | 印刷規則能不能搬到本站的三關：有對象嗎／分母存在嗎／量得到嗎。三關全過寫成規則，過兩關寫成注意事項 |
| **墨跡外框 / ink box** | 一張卡上所有非表面像素的外接矩形。**「主體佔版面」量的是這個，不是深色像素比例**——純文字卡的深色像素永遠到不了門檻 |
| **微字 / microcopy** | 一個載體上最小的字級。**它是級距規則的分母**：頁面上是 `eyebrow` 12px，卡上今天是 kicker 17px |
| **檢查缺席的規則** | 判斷「該寫的有沒有寫」而不是「寫的有沒有違規」。本檔只有規則 31 一條，判準是清單不是模式 |

---

## 決策紀錄

每條規則背後的依據或代價。**改規則之前先讀對應那條**——下面每一項都是某次「順手改一下」造成的。

| # | 決策 | 依據 / 代價 |
|---|---|---|
| 1 | 設計規範集中在這一個檔案 | 先前散在 `NOTES.md`、`input.css`、`tailwind.config.js`，其中兩處已與現況不符且沒人發現 |
| 2 | 以 `www.spacex.com` 的實測值為準 | 「風格接近」無法檢查，數字可以。**已由 #23 取代** |
| 3 | 單一墨色 + alpha | 參考站實測只有 `#F0F0FA` 一色。內文放 0.8 而非 0.9，是為了維持與遷移前 `#c8c8cc` 同階的抗光暈亮度。**內文的數值已由 #139 改為 0.85（決策 #46）**；「單一墨色 + alpha」與「不放到 0.9」兩點仍然成立，過期的只有 0.8 這個數字 |
| 4 | 中文走系統字體 | 繁中網頁字體切片後仍數百 KB 且 swap 重排。**Windows 的中文比 Mac 差一階，已知並接受** |
| 5 | `line-height 0.9` 只給拉丁 | 中文字身滿框，沒有 descender 空隙，0.9 會裁到筆劃 |
| 6 | 英文領銜 + 中文副標 | 中文沒有大寫，參考站的標題簽名只能由拉丁行承載。**代價：H1 的主要文字變成英文**，長文內部 H2 因此保留中文 |
| 7 | 小型大寫收到 `0.09em` | 實測值。遷移前的 `0.2em` / `0.25em` 約是參考站的兩倍 |
| 8 | 按鈕字距收成 `0` | 實測值。**代價：拉丁簽名的三個承載點少一個** |
| 9 | 圓角只給控制項 | 參考站實測（控制項 4px、縮圖 8px、社群鈕正圓）。遷移前的全域 0 比參考站更嚴格 |
| 10 | Tailwind 未用到的 `borderRadius` 階不能刪 | 刪掉後後來寫的 `rounded-md` 會落回 `0.375rem`，方角規則無聲破掉 |
| 11 | 自架 Roboto Mono | 等寬已在 7 個模板出現 33 次 + 24 個 code 區塊，卻交給系統堆疊——三台機器三種面貌，那樣不算設計的一部分 |
| 12 | 內容區塊不畫外框 | 「卡片是髮絲框」讓 14 條路由累積出 162 個全框方塊，其中 49 個是框中框 |
| 13 | 不用捲動揭示動畫 | 參考站實測 0 個。本站也有前科：IntersectionObserver 版本讓首頁 19 個元素永久隱形 |
| 14 | 移除進場逐層淡入 | 參考站沒有，且它讓 LCP 元素的可見時間晚 100–300ms |
| 15 | 不使用攝影 | 沒有火箭。加上 CSP `img-src 'self'`、對比稽核在背景圖上是瞎的、滿版影像會成為新的 LCP 元素 |
| 16 | 長文頁不切滿版屏 | 參考站沒有長文頁。切屏會同時毀掉連續閱讀與 `Ctrl+F` |
| 17 | τ 曲線呼吸用計時器不用 rAF | rAF 會讓主執行緒以螢幕更新率被喚醒卻什麼都不畫 |
| 18 | hero 曲線用遮罩淡出而非底板 | 這套語彙沒有底板；卡片是髮絲外框，永遠不是填色 |
| 19 | 規則配 CI 閘門 | 這個 repo 已經實證過靠 review 守不住：CSP 一年未生效、hex 規則寫了就破、55 個無效 class |
| 20 | `build-logo.js` 的白色不跟著改 | 結構化資料 logo 用在淺色底，與站內墨色無關。曾經有人把「深色底用的白色標記」誤當成「深色的 logo」放進 JSON-LD，Google 收到一張白底白字 |
| 21 | 章節索引的捲動高亮**不是**捲動揭示 | 決策 #13 禁的是「文字的可見性取決於腳本跑成功」。索引是十條真的錨點連結：腳本不在、載入失敗或拋錯，十條仍完整可讀、可點、可跳，失去的只有「知道自己在哪」。腳本只搬移 `aria-current`，樣式直接讀那個屬性——**一份狀態，不是兩份**。豁免以行為性質界定並寫在這裡，與 `#menuOverlay` 同一個理由：第二個豁免不可能在沒人開口的情況下被加進去 |
| 22 | `<main>` 用 `overflow-x-clip` 取代 `overflow-hidden` | `overflow: hidden` 會讓元素成為捲動容器，**其中的 `position: sticky` 因此全部失效**——實測捲 3000px 後 sticky 元素從 706px 移到 −2294px。`clip` 不建立捲動容器，保留水平裁切而不破壞 sticky |
| 23 | 換參考站至 `jules.google` 行銷層，並**反轉**裁決規則 | SpaceX 的任務（建立有紀律的骨架）已完成；導入 Jules 是為了補上技術性格。若照舊「參考站為準」，深紫底、圓角、多彩、吉祥物會全數翻盤，那是換品牌不是改版。**取代決策 #2** |
| 24 | 只取 Jules 行銷層，排除產品層與文件站 | 實測確認三個表面是三套設計語言。不指明會抄錯對象 |
| 25 | 取行銷層的形式、產品層的語氣 | 參考站的 8bit 在做去風險化，因為它的產品會自動改寫使用者的 repo。本站賣專業判斷，且沒有第二層可以校正語氣。**代價：放棄 8bit 最討喜的角色與玩心** |
| 26 | 不做 ASCII 字標，不做吉祥物 | 兩者都是參考站可辨識的商業外觀。本站已有 24 個 canvas 且要將其點陣化，**點陣化的 canvas 加上 ASCII 字標會非常靠近前者**。代價：放棄一個確實有效的視覺裝置。但視覺資產必須累積在拓思身上 |
| 27 | 加入 phosphor，本站第一個非墨色 | 點陣質地在嚴格單色下容易讀成「髒」而非「有質地」，需要一個維度區分結構與雜訊。**代價：「單一墨色」從此有一個例外，而例外會誘發第二個例外。上限五個元素就是防止那件事的閘門；若上限被鬆綁，這條必須重寫** |
| 28 | 選琥珀而非綠或青 | 唯一與冷白墨色色溫互補者。綠語意陳腔濫調且在台灣商務語境有政治聯想；青與冷白區隔度低且接近參考站三色之一。**代價：對比度 10.8:1 低於綠的 14.6:1，餘裕仍有 2.4 倍** |
| 29 | 等寬字擴大到機器輸出，但不碰導覽、按鈕與標題 | 判準是「這段文字是不是被量出來或列出來的」。導覽與按鈕是品牌聲音。**代價：站上同時存在兩種字體邏輯，作者每次都要判斷一次；判準寫成一句話就是為了降低這個成本** |
| 30 | box-drawing 用同 family 的第三個 `@font-face` | advance 差 0.016%，可透明混入任何等寬文字流，模板零改動。**代價：筆畫比 Roboto Mono 重約 37%。次選 Noto Sans Mono 筆畫更貼近，但縱向跨距只有 1.213 em，必須釘死行高——選擇不釘** |
| 31 | Departure Mono 只用於 ASCII 區塊，且鎖 11px 倍數 | advance 寬 6%，混入文字流會把每一欄剪歪；座標落在 50 單位格線，只有 11／22／33px 像素完美。**代價：這些區塊不參與響應式型階。這是真點陣字的本質，不是缺陷** |
| 32 | 點陣紋理不得用 `background-image` | 對比度檢查器遇到祖先帶 `background-image` 會**跳過**該元素——不會變紅，而是變盲。決策 #15 早就用同一個理由拒絕照片。**代價：某些紋理用 CSS 漸層更簡單，改用字型字符或 inline SVG 較費工。且規則讀模板，`input.css` 裡引入的漸層在它視野之外** |
| 33 | τ 曲線只換 rasteriser，不換數學也不換排程 | 一個渲染層的改動覆蓋 24 個 canvas 與 18 張分享卡，且不觸碰決策 #17。**代價：點陣化後精細度下降，網格限制 4–6px 就是為了讓「二階系統步階響應」這個資訊不流失** |
| 34 | 新規則以關閉狀態進場 | 本站既有慣例。規範裡的規則若沒有對應的程式碼骨架，就會退化成沒人遵守的形容詞。**代價：repo 裡會有一段時間存在關著的規則，且必須先補上 `turnedOnBy` 這條死路** |
| 35 | **不為「mono 堆疊必須含 CJK 字體」寫檢查規則** | 堆疊本身解決了 eyebrow 與表格的中文問題，不需要逐元素例外。但守住它的只剩 `tailwind.config.js` 的一段註解。`check:design` 明文只讀模板與 `site.toml`，斷言放不進去；放進 `check:classes`（它讀建置後的 CSS）技術上可行，本次選擇不做。**代價：這正是本文件開場反對的東西——一行 regression 就讓 95 個 eyebrow 在 Windows 上變細明體，而那在 Mac 上完全看不見。這是明知代價的選擇，不是疏漏** |
| 37 | header 與 footer 共用一份 `_nav-columns.html`，但 **Company 欄不共用** | 四個主題欄逐字相同，抽出來就不會再分歧。Company 欄不同**是刻意的**：footer 的品牌區塊已經有一個連到 `/` 的 TauX 字標與一個 GitHub 圖示，所以它的 Company 欄故意省略 Home 與 GitHub。把兩份強行統一會讓 footer 連兩次 `github.com/taux-io`——實測確認過。**看起來像 drift 的東西其實是一個沒有人寫下來的決定**，這條就是把它寫下來。代價：Company 欄仍是兩份，仍可能分歧，而沒有東西檢查 |
| 38 | **表格欄位標題暫不改等寬**（`th` 基礎規則試過並退回） | 看起來是乾淨的一條規則，實測撞到兩件事：`th` 的瀏覽器預設字重是 bold，套上 `font-mono` 會渲染成 **Roboto Mono 700**，而本站自架的 Roboto Mono **只有 400**——那是合成假粗體，正是字級 token 把字重寫死要防的事；而且六個 `th` 已帶 `font-display` utility，utilities 勝過 base，三十個裡只有二十四個會生效。**代價：欄位標題與 eyebrow 現在是兩種字體**，直到規範決定標題的層級由什麼承載 |
| 39 | `.tag` 保留 `font-display`，`.eyebrow` 改 `font-mono` | 同一個 `text-eyebrow` token 現在有兩種字面，這是刻意的：eyebrow 標示區段，是機器輸出；tag 標示服務名稱（`GEO Service`、`Who We Are`），是品牌聲音，與同樣留在 D-DIN Condensed 的導覽和按鈕同類。**代價：兩者並列時看起來像渲染錯誤，而沒有東西檢查這個區分** |
| 42 | 網格圖形的 idle 重畫要先問「輸出會不會變」 | 點陣網格每秒要畫幾十萬個 `fillRect`，而呼吸的振幅是 1%——多數幀畫出來的方格與上一幀**完全相同**。實測未加判斷時，6× CPU 節流下主執行緒有一半時間在忙，永遠不停，只為了一張背景圖。加上「量化後的幾何沒變就跳過」之後：`fillRect` 從 815,928/秒降到 23,022/秒，延遲 p50 從 25.1ms 降到 0ms。連續描邊版本不需要這個判斷，因為它只是幾十個原生路徑呼叫——**換成網格之後，這個判斷從最佳化變成必需品**。決策 #17 的理由不變：計時器照跑，只是通常直接返回 |
| 41 | ASCII 區塊的每一行必須等寬，而且要用量的（**由 `npm run geometry` 執行**，issue 148） | 404 的框第一版右邊界對不齊：邊框 26 字、內容行 28 字。**用眼睛看不出來是差兩個字還是字型沒對齊**——Departure Mono 與 box-drawing 子集的 advance 差 6%，兩種原因長得一模一樣。改法是量渲染後每一行的寬度（現在五行都是 364px），不是數字元 |
| 40 | mono 堆疊裡 CJK 字體排在**拉丁等寬之後** | 排在前面的話 PingFang TC 會搶答 `→ ≥ ≈ ∞ ● ★`——它們不在 Roboto Mono 子集裡——並給出**全形** advance：`→` 在 20px 下從 12.04px 變成 20.0px，`adk-skill-patterns` 手工對齊的 ASCII 流程圖每行位移 5.6px。排在拉丁等寬之後，符號由 SF Mono 回答而字寬守住，中文仍然落到 PingFang（前面那些字體都沒有 CJK）。**副作用：這讓改動的實際影響縮小到 Windows**——Mac 上中文本來就會落到 PingFang |
| 43 | **首頁領銜句 `Empower Your Business with AI` 是語域規則的具名例外**（issue #133） | 這句話直接違反決策 #25——它是行銷句型，動詞空泛、沒有可驗證的主張，而本站訂的是「取產品層的語氣」。**裁決是行銷主張優先，理由是對外口徑一致性**：名片、簡報、提案用同一句，官網不能是唯一的例外。這條必須寫下來，因為語域規則**沒有任何檢查器守著**——不寫的話，下一個讀這份文件的人（或 agent）看到那句話，唯一合理的反應就是把它「修好」。同一頁的引言（「重要的不是你說了什麼，而是 AI 聽懂了什麼」）與 τ 字源段落刻意一字不動，它們是這頁維持精確感的錨。**代價：語域規則從此有一個例外，而例外會誘發第二個例外**——比照決策 #27，下一個要求破例的人必須先在這裡新增一列 |
| 44 | **推翻「版面不變」，滿版只留 404**（issue #137） | 全站曾有 **15 個區塊**（6 個模板）釘在 `86vh` 並垂直置中。三行字的區塊與三十行字的區塊各佔一屏，捲動出來是一串等長的脈衝，讀者無從感覺哪一段重要——這是「看不下去」的主因，字級只是次因。**這條推翻的是本文件自己訂的規則**，而 DESIGN.md 的裁決規則是「衝突時本站現行規則為準」，所以推翻需要具名授權：使用者在被告知該規則存在後仍明確要求全部拿掉，包含各頁 hero。垂直置中隨之移除，因為最小高度消失後它不再有作用，留著等於用兩套規則決定同一件事。間距收斂成兩階並寫進版面章——這不是新規則，是把既有的隱性做法（延伸閱讀早就用較小的間距）寫明。**代價：背景 τ 曲線的視差行程綁的是區塊高度的比例，區塊變矮，曲線更接近靜止。已知並接受**（不變式仍然成立，不會露邊） |
| 45 | **內文三階一起上移，且不用 root font-size 縮放**（issue #138） | 16px 內文配深底大量中文，讀十分鐘會累。但**只抬 `base` 會把層級壓扁**：17px 離 Tailwind 原生 `lg` 的 18px 只剩 1px，花力氣抬了字級卻犧牲了層級；抬 `lg` 到 19px 之後，同一個擠壓原封不動移到 `xl`（20px），所以 `xl` 也得讓位到 22px——而 `xl` 正是 `.display-sub` 在手機寬度穿的那一階，是手機上最需要讀清楚的一行。**明確否決的替代方案是在 root 放大 font-size**：那會讓所有 rem 間距與 `max-w` 同步放大，而 #137 剛剛才刻意收緊那些留白，兩招互相抵銷。`display-sm`（內頁 h2）取 2.25rem 而非 2.5rem，`--ink-body` 取 0.85 而非 0.9（決策 #46）——**這次同時動了版面、字級、行高與墨色四件事，每一項都取範圍上限的話，出問題時沒有東西可以歸因** |
| 46 | **內文墨色 0.8 → 0.85，且為它寫規則**（issue #139） | 0.8 是決策 #3 為了「密集中文在近黑底的暈光邊界」刻意留的，實測顯示那個邊界還有餘裕：長段中文在 0.8 下讀久了糊成一片，而 0.85 算出來是 `#CECED6`，對三個表面分別是 **12.59:1**（`--surface`）、**13.16:1**（`--surface-deep`）、**12.28:1**（`--surface-raised`），全部是 AA 門檻 4.5:1 的兩倍以上（0.8 時是 11.18 / 11.62 / 10.93）。**這三個數字是算出來的，不是估的**——本檔開場那條「目視得出來的數字也要用工具數過」同樣適用於對比度。**停在 0.85 不到 0.9**：0.9 就是 `--ink-muted`，三階墨色會被靜默壓成兩階，那是另一個決策不該當副作用發生。這個數字**寫在兩個檔案裡**（`input.css` 的變數與 `tailwind.config.js` 的硬寫值），因為 `<alpha-value>` 表達不了預設 alpha；改漏一邊是靜默失敗，所以配了「ink body single source」規則。**副作用是本檔第一條讀 `input.css` 與 `tailwind.config.js` 的規則**——決策 #35 拒絕檢查 mono 堆疊的理由之一（「斷言放不進去」）從此不成立。#35 沒有因此翻案（字體堆疊順序不是純量，仍需另一種斷言），但**它的理由已經過期，留著不修正就是這份文件反覆警告的那種漂移** |
| 47 | **`?v=` 由 generator 從 CSS 內容算出，不再手動遞增**（PR 145） | 它從 Go 遷移之後**一次都沒動過**，跨越每一次調色盤、token 與字級改動——包括同一個下午上線的兩支 PR。`_headers` 的註解自己承認這是「沒有東西強制遞增」的設計，賭的是有人會記得。**代價是有界但真實的**：`/static/css/*` 快取一小時，回訪者拿到新 markup 配舊樣式表；而當改動引入了新的 utility class，穿著它的元素會退回舊規則說的樣子——`hidden sm:flex` 對上一份沒有 `sm:flex` 的樣式表，就是在所有寬度都隱藏。**解法是讓失敗不可能發生，而不是加一條提醒**：查詢字串是那個檔案的函數，不可能落後於它。用 FNV-1a 而非密碼學雜湊、且不新增相依：這裡要的是「位元組變了就變」，不是抗碰撞。`css version is derived` 規則守著它不被「簡化」回字面值。**副作用**：模板註解裡的票號從此要寫成 `PR 145` / `issue 133` 而不是帶井字號——三位十六進位數字本來就是合法色碼，`zero hex` 規則抓它是對的（本條的註解草稿自己被抓了兩次），放寬規則會讓它對真正的 `#123` 失明 |
| 48 | **靜止不可見的元素必須 ship inert，而且義務綁在豁免清單上**（issue 143） | `opacity-0` + `pointer-events-none` 擋得住眼睛與滑鼠，對鍵盤**完全無效**：`#menuOverlay` 關閉時仍有 19 個連結加 1 個關閉鈕留在 Tab 順序裡，實測按 Tab 到第 4 下就落進一個看不見、也不畫焦點環的選單。決策 #21 給了它 `opacity-0` 的結構性豁免，但那條豁免只講眼睛，**沒有講鍵盤**——這條補上另一半。`inert` 是唯一能修這件事而不動 DOM 內容的屬性，而 DOM 內容留著正是這個覆蓋層存在的理由（爬蟲要看得到；實測服務出去的 HTML 仍有 19 個 `<a href>`）。**寫在 markup 裡由腳本移除，不是反過來**：腳本沒跑時選單打不開，一個打不開的選單不該收集焦點。**規則綁在 `OPACITY_EXEMPT_IDS` 這同一份清單上而不是另開一份**——豁免與義務同源，下一個豁免不可能只繼承權利不繼承責任 |
| 49 | **grid 與 flex 子項預設 `min-width: auto`，容不下不能收縮的內容**（issue 151） | 三條路由的內容被 `<main>` 的 `overflow-x-clip` 靜默裁掉，手機上直接看不到。成因不是版面寫錯，是 CSS 的預設：**grid／flex 子項的 `min-width` 是 `auto`，也就是內容的 min-content 寬**，所以裡面放一個不能換行的東西（`<pre>`、圖表 canvas、長 URL）時，子項不會縮，會撐破容器。內層即使已經有 `overflow-x-auto` 也救不了外層。解法是子項加 `min-w-0`。**這條沒有寫成 `check:design` 規則**，因為「哪些子項需要」取決於內容能不能收縮，讀 class 字串判斷不了；守著它的是 `npm run geometry` 的實測。**同時修正了那個檢查自己的兩個缺陷**：它把被「頁面外殼以外的祖先」裁切的元素也算成溢出（`/what-is-prompt-injection` 的裝飾光暈，父層 `<section>` 早就有 `overflow-hidden`——作者刻意界定，不是缺陷），以及它會抓到 Chart.js 尚未完成尺寸計算的暫態（376px，實際落在 176px），**後者會讓 gate 隨機變紅，而隨機變紅比沒有 gate 更糟** |
| 50 | **按下回饋、44px 觸控目標與三個使用者偏好**（issue 154） | 依 Apple 的 WWDC 設計談話對全站逐條稽核的結果。**大部分不適用**——skill 有一半以上在講直接操作（拖曳、速度交接、動量投射、橡皮筋），而本站沒有任何可拖可滑的介面；另有四條（彈簧、`box-shadow`、捲動揭示、觸覺）與既有規則正面衝突，**明確不採用**。真正的缺口只有七個，全在「回饋與無障礙」。**最重的一條是全站 8 個 `:hover`、0 個 `:active`**——觸控上等於按下無回饋。順帶把 `.btn` 的 500ms 轉場收到 200ms：它是全站唯一的 500ms（22 個 `duration-200`、17 個 `duration-300`），而參考站實測基準是 `.15s`。**三個使用者偏好先前只有 `prefers-reduced-motion` 且只做在 JS 層**，CSS 的轉場全部無視它。`prefers-reduced-motion` 的實作刻意只縮短時間、不碰可見性——決策 #13 的前科正是「因為某個機制而讓 19 個元素永久隱形」，用無障礙當名義重演同一件事會更糟。**代價**：元件層從此多一條位置規則（所有 `:active` 必須在最後），而**沒有東西檢查它** |
| 51 | **採用 Apple 動態與材質語彙，推翻彈跳／陰影／捲動揭示三條禁令**（issue 156） | 使用者在被明確告知代價後選擇全做：**這是換一套設計語言，不是調整**。被推翻的三條理由**沒有刪除**，只是被更高層的決定蓋過——它們仍然是理由。**彈跳**：只用於「抵達」（覆蓋層開啟），這是 `--ease-arrive` 全站唯一的使用處；Apple 自己的說法是 overshoot 屬於動量之後，而本站沒有手勢，所以其餘一律臨界阻尼。**陰影**：三階，依「大表面讀起來更厚」分配給導覽、覆蓋層、按鈕，各自搭配上緣的 inset 髮絲線——深色材質上是上緣的受光讀成「表面」，只有陰影會讀成「髒污」。導覽底下的 1px 髮絲線改成漸層捲動邊緣。**捲動揭示**：決策 #13 的禁令推翻、事故沒有忘記，機制方向反轉（見版面章）。**代價**：本站從此有兩種 easing、三階陰影、以及一個依賴 JS 才有的視覺效果——三者都是先前刻意沒有的東西，而維持它們不退化的責任現在落在 DESIGN.md 與五條失敗路徑的實測上 |
| 52 | **新語彙一落地就配規則，不留給下次記得**（issue 156 的後續） | #156 給了本站兩種 easing、三階陰影與一個按下狀態——**全部是先前刻意沒有的東西，而且沒有任何東西檢查**。本檔開場的論點是「規則寫了沒有東西檢查，就等於沒有規則」，而一套語彙只有在夠小的時候才是語彙：本站原本只有一個墨色、兩種髮絲線、零陰影，理由從來不是品味，是**沒人管的刻度會為了某個前三階都不合的情況長出第四階**。四條新規則：`easing scale`、`shadow scale`、`press follows hover`、`reveal ships visible`，五種失敗模式各自弄紅驗證過。**寫規則時翻出兩個先前的錯誤**：站上其實有**三種** easing（`.btn` 與 `.btn-quiet` 還留著 easeOutExpo），以及 `.btn-quiet` 也是 0.5s——而 #154 的稽核與 commit 都宣稱「`.btn` 是站上唯一的 500ms」。**兩處都是半套修正被當成整套宣告**，現在一起收斂到 token |
| 36 | 落地切成三階段、三次合併 | 合併即上線且 CI 擋不住。階段 ① 對訪客完全不可見（只新增 token、字型與關著的規則），風險為零卻讓後兩階段有東西可依靠；階段 ② 是唯一改變視覺的一次，單獨上線才能歸因。**代價：三次 PR 的流程成本** |
| 53 | **品牌重置：參考站換成航太行銷站的設計分析，裁決規則反轉回「參考站為準」** | 使用者在被逐條告知代價後選擇全做——**這是換一套設計語言，不是調整**，與導入 SpaceX 那次（決策 #2）同一個形狀，取代決策 #23。被推翻的舊決策：**#3**（單一墨色＋alpha → 純白單值）、**#15**（不使用攝影 → 攝影成為規範的一部分，但本次選擇不放圖）、**#16**（長文頁不切滿版屏 → 章節封面屏切屏，正文仍連續）、**#23**（裁決規則 → 反轉）、**#26**（不做點陣字標 → 點陣語彙整體移除）、**#27**（phosphor → 移除）、**#29**（等寬字治理 → 等寬字整體移除）、**#31**（Departure Mono → 移除）、**#33**（τ 曲線 rasteriser → canvas 整體移除）、**#39**（eyebrow 與 tag 的字體分工 → 兩者同字體）、**#40**（mono 堆疊的 CJK 順序 → 堆疊不存在）、**#41**（ASCII 逐行等寬 → 斷言刪除）、**#44**（滿版只留 404 → 每個 band 佔滿一屏）、**#45**（內文三階 17/19/22 → 16px 兩階）、**#46**（內文墨色 0.85 → 純白）、**#51** 的陰影與捲動揭示兩項（→ 移除；easing 收回一種）。**每一條的理由都沒有刪除，只是被更高層的決定蓋過——它們仍然是理由。** ⚠️ **最重的代價是交乘出來的，不在任何單一決定裡**：純黑表面、純白內文、16px 型階三者分別推翻 #3、#45、#46，而那三條各自的理由**都是同一件事**——密集中文在近黑底上的暈光。合起來是 21.00:1 的對比配 16px 配 39,456 字中文。**回調順序寫在這裡**：真的糊了，調的是墨色 alpha，**不是型階**——型階回調會再壓扁一次層級，那是 #45 踩過的坑。另一項具名放棄：不新增 80px display 階，等於換了語彙卻少一個簽名承載點，這是使用者明確選擇的 |
| 54 | **同一天的第二次品牌重置：參考站換成 Apple 網站的設計分析** | 使用者在被逐條告知代價後選擇全做。**兩次重置的參考站互相矛盾**——上一版是純黑無強調色、全大寫、負字距剛被刪除；這一版是純白、Action Blue、句首大寫、負字距回來。被推翻的決策：**#11**（自架字體以消除平台差異 → 採 SF Pro 並接受 Windows 上不是 SF Pro）、**#53** 的多數具體值（表面、墨色、字距、大小寫、圓角、封面屏高度）。⚠️ **參考站的實測值不等於本站可以用的值**：新檔的 `ink-muted-48` `#7a7a7a` 在白底是 4.29:1，**低於 AA**，照抄會讓 `contrast` 第一次執行就紅；本站取 `#707070`（4.95 / 4.55，唯一兩者都過的值）。這一條是本版最重要的教訓，寫在開場的自白裡。**明知的代價**：三個圓角階（`xs`、`md`、`full`）在本站沒有使用者，而決策 #52 的論點是沒人管的刻度會長出第四階——這裡反過來先有刻度後有使用者，守著它的是 `radius scale`。**三小時前才寫的東西這一版就刪掉**：`geometry` 的 `cover` 探針（封面屏不再滿一屏）。**沒有被推翻的**：四個架構模組（`stylesheet`、`walk`、`routes`、`states differ`）與守著它們的三條規則——它們問的是東西寫在哪、怎麼走訪、誰讀，與設計語言無關，而這是兩次重置之間唯一沒有動過的東西 |
| 55 | **每個 locale 一組 CJK 字體堆疊，推翻決策 #4 的單數假設** | 舊堆疊的三個 CJK 面（PingFang TC、Microsoft JhengHei、Noto Sans TC）**全是繁體**。日文的漢字會由 PingFang TC 回答，而 直、骨、令、海、説 的中日字形不同，日文讀者一眼看得出來；簡體同理；韓文諺文 PingFang TC 沒有，掉到 `sans-serif`。決策 #4 的「中文走系統字體」在只有一種中文時成立，五個 locale 之後不成立。改由 `html[lang]` 換 token。**代價**：`contrast` 與 `geometry` 現在要在五種字面下各跑一次，而**沒有東西量得出這台機器看到的是不是對的那一面**——與決策 #54 對 SF Pro 認下的是同一個缺口，只是現在有五個 |
| 56 | **`heading structure` 條件化，而不是關掉** | 決策 #6 的依據是「中文沒有大寫，標題簽名只能由拉丁行承載」。**那個理由在 `en-US` 上不成立**——領銜句就是內容本身，硬配副標會產生一個沒有內容的第二行。`display-lead` 每個 locale 都要，`display-sub` 只在非拉丁 locale 要求。`check:entity` 的「title 與 description 含中文」同步改成「含該 locale 的文字」——**它現在斷言的是一個它以為只有一種的東西**。代價：規則的條件多了一個維度，而條件化的規則比無條件的規則更容易在下一次被讀錯 |
| 57 | **接受分享卡的字面由建置機器決定，不為它自架字體也不做像素基準** | 本檔的「沒有東西檢查的事」已經記著這個缺口，並列出唯一的兩條出路：為卡片自架字體（決策 #54 選擇不做），或比對渲染後的 PNG 與 commit 的基準。五個 locale 把 20 張卡變成 100 張，**日文卡印出中文字形是圖片，改不了也看不出來**。使用者在被逐條告知之後選擇**接受並記錄**，與決策 #54 對 SF Pro 的處理同一個形狀。**代價是真的且會停在每一次分享上**——記在這裡是為了讓它是一個決定而不是一次疏漏 |
| 58 | **每一條路徑都有 locale 前綴；`/` 的轉址是 302 且依語言，其餘 20 條是不依語言的穩定 301** | ⚠️ **這一格的標題原本寫「根路徑只放 301，而那個 301 不依語言」，而那是實作前的計畫，不是上線的東西。** 上線的 `/` 是 302，PR #208 改的，理由就寫在下面這段裡——也就是說**標題與它自己的內文相反了一段時間**。決策紀錄的標題是給人掃讀的，掃讀的人不會往下讀那一段，所以錯的標題比錯的內文傳得遠。改標題而不是刪那段推理：推理沒有錯，是標題停在推理之前的那一版。理由如下——使用者選擇所有路徑都帶前綴（`/geo-guide` → `/zh-Hant-TW/geo-guide`），並選擇依 `Accept-Language` 自動導向。這兩件事合起來有一個協定層的硬後果：**301 是永久且可快取的**，瀏覽器會把目的地記住，之後換語言照樣去舊的那一個；`Vary: Accept-Language` 理論上該擋住，而瀏覽器對「帶 Vary 的已快取 301」的處理不可靠。所以裁決是**分開**：20 條已索引路徑走**穩定的 301**（不依語言，權重乾淨轉移），`Accept-Language` 只作用在 `/` 一條路徑上並用 302。**代價**：`/geo-guide` 從此是轉址而不是頁面，20 條全部要進 `[[redirect]]`，`check:routes` 的 ledger 逐條盯著 |
| 59 | **bot 在 `/` 豁免導向，而豁免的是導向不是內容** | 自動導向會讓 Googlebot 在 `/` 被送去某一個語言，而 `/` 是全站最高價值的 URL。豁免依 User-Agent 判斷——**這只差一步就是 cloaking**，所以分界線寫死在這裡：送給 bot 的位元組必須和送給正典 locale 使用者的**相同**，差別只有跳不跳。依 User-Agent 給不同內容是 cloaking；依 User-Agent 決定跳不跳不是。**這條必須寫下來**，因為下一個要求破例的人會拿這條當先例。⚠️ **而它一度沒有做，這才是這條決策真正的教訓。** 從 #200 落地到現在，`src/worker.js` 裡**沒有任何 User-Agent 判斷**，所有請求（含 Googlebot）在 `/` 都拿 302——#200 的驗收條件明寫「送 Googlebot UA 到 `/` 拿到 **200 而不是導向**」，那條從未成立。更糟的是**驗收條件被改寫成別的東西**：`src/worker.js` 的檔頭寫著「NO USER-AGENT CHECK, AND THAT IS THE POINT」，`NOTES.md` 抄了一份，而 `scripts/visual/contract.js` 的註解把本條寫成「依 UA 給 301／302 就是 cloaking」——**本條說的是相反的話**。三個檔案互相引用同一個改寫，讀起來像是這件事被考慮過並且做了決定。**換掉條件不是滿足條件**，而這條決策之所以沒有被檢查器守著，正是因為那個「守著它」的斷言被寫成了守另一件事。現在 `contract` 正面斷言 #200 的原文：Googlebot UA 到 `/` 回 200 且 body 與 `/zh-Hant-TW` 逐位元相同，一般瀏覽器 UA 到 `/` 回 302 |
| 60 | **字形轉換交給工具，詞彙表自己控** | 繁轉簡是兩件事，只有一件是機械的。字形是固定映射；**詞彙不是**，同一個東西台灣和大陸常常不同名，逐字轉出來的頁面是「繁體中文穿簡體字」，比兩者都糟。OpenCC 有現成的詞彙表（`twp` → `cn`），而它在本站語料上**九個詞是錯的、且都很高頻**：`核心` → `内核`（`核心使命` 變成「內核使命」）、`執行` → `运行`（`政策執行` 變成「政策運行」）、`稽核` → `审核`（那是審查不是稽核）、`查詢` → `查找`、`向量` → `矢量`、`複製` → `拷贝`、`建立` → `创建`、`程序` → `进程`、`開啟` → `打开`。所以分工是**工具轉字形、`scripts/hans-terms.js` 決定詞彙**，八十幾個詞逐條可讀，刻意不換的九個連同理由一起寫下來。⚠️ **最長優先排序擋不住專有名詞**：把名字映射成自己不會移除較短的觸發詞，`資安` 照樣把「後量子資安產業聯盟」改成「後量子網路安全產業聯盟」——那是 `/pqc-migration` 的引用來源，被改掉的是「這個說法出自誰」。專有名詞改用哨符換出來再換回去。**代價**：這張詞彙表沒有東西檢查它，它的正確性靠人讀 |
| 61 | **`inLanguage` 分兩種節點，共用的那個列清單** | `header.html` 的 WebSite 節點寫死 `"inLanguage": "zh-Hant-TW"`。它是共用模板，所以**二十頁英文頁全部宣告自己是繁體中文**，而這在線上已經如此。改成 `{{ locale }}` 是錯的解法：那個節點的 `@id` 是全站唯一且不帶 locale，同一個 `@id` 依讀取頁面不同給出不同語言，是把矛盾寫得更清楚而不是修掉。裁決：**頁面層節點用該頁 locale，全站唯一的節點列出所有已發布語言**，清單由 generator 從各頁的 locale 表推導（名冊是顯示順序，不是已發布集合）。新規則 `inLanguage tells the truth` 守著，它一開就抓到另外三處寫死的、包含一個 `zh-Hant`——一個站上其他地方都不會匹配的值 |
| 62 | **分享卡的 CJK 堆疊跟著卡片的語言，決策 #55 漏掉的那一半** | #55 給每個 locale 一組 CJK 面，理由是舊堆疊三個全是繁體、日文漢字由 PingFang TC 回答時字形明顯不對。那次修的是 `src/input.css`，**`scripts/assets/build-og.js` 沒改**——卡片留著網站剛剛才丟掉的單數假設，於是日文卡會**依構造**印出中文字形，而不是靠建置機器的運氣。決策 #57 接受的是「不知道這台機器有哪個面」，不是「明知道要哪個面卻不去要」。改成依卡片的 locale 換 `--cjk`。實測有效：日文卡現在是 Hiragino Sans 的假名與漢字，韓文卡是 Apple SD Gothic Neo 的諺文——後者先前 PingFang TC 根本沒有諺文，會掉到 `sans-serif`。⚠️ **這只在建置機器有那個面時成立**，而卡片是在誰的機器上建的決定了答案，那正是 #57 記下的缺口，沒有被關掉 |
| 63 | **`/` 的語言協商做成 Worker，推翻 issue #200 明文的「不加 Worker `main`」** | #200 寫死「**Cloudflare zone 的 Redirect Rule，不加 Worker `main`**」，理由是 `_headers` 對 Worker 產生的回應不生效，14 條標頭規則要手動重貼、忘記的那條靜默失敗——而這個 repo 已經被同一件事咬過（CSP 寫在從未進入運行拓撲的 `nginx.conf` 裡，宣稱了數個月、從未送出）。**推翻它的是兩個在動手前才驗出來的事實**：一、`http.request.accepted_languages`（Cloudflare 幫你依 `q=` 排好的陣列，正是這件事要的東西）**只在 Transform Rules 可用**，Redirect Rules 只讀得到生的標頭，能做的只有比對前綴——`zh-TW;q=0.1, en;q=0.9` 會答錯，而 #200 自己要求「動手前先確認運算式真的讀得到 `Accept-Language`」，那次確認的結果就是讀不到。二、zone 規則**活在後台、review 看不到，而且只有 `BASE_URL` 指向 production 時契約測試才驗得到**，等於上線之後才知道對不對；HSTS 已經是這個形狀，`NOTES.md` 明寫那違反本專案自己的偏好。**代價要誠實寫**：`_headers` 對 Worker 產生的回應不生效這個危險**一點都沒有消失**，它是被「這支 Worker 不自己組回應」的寫法迴避掉的——它向 assets 層要那個本來就會送出的回應，只改標頭。而**沒有任何東西檢查未來的人會不會在那支 Worker 裡直接 `new Response("…")`**：沒有規則、沒有斷言，只有 `src/worker.js` 檔頭那兩段散文，加上 `run_worker_first` 只有一條路徑所以爆炸半徑是 `/`。**這正是本檔開場反對的形狀**（規則寫了沒有東西檢查就等於沒有規則），這裡明知並接受，寫下來讓它是一個決定而不是一次疏漏 |
| 64 | **韓文的文件名統一用 `『』`，而「條目名」仍然是 `「」`** | `templates/ko-KR/pqc-migration.html` 同一頁兩種寫法指同一份文件：內文 4 處 `『』`、JSON-LD 2 處 `「」`（issue #242）。兩種都合 2015 修訂的韓文標點規範，所以這不是 bug 是決定。裁決依規範的分工而不是依「哪個多」：`『』` 겹낫표給**獨立發布的文件**（書名、報告、指引），`「」` 홑낫표給**小標題與條目**。金管會那份《금융업 포스트 양자 암호 이행 참고 지침》與數位產業署那份都是獨立發布的文件，所以 JSON-LD 那兩處跟著內文改成 `『』`。⚠️ **同一頁的 `「PQC 정책과 거버넌스」` 刻意不動**——它是指引**內部的一個方針條目**，不是文件，`「」` 本來就對。**這一格真正要記的是分界而不是結果**：下一個看到「同一頁兩種括號」的人，第一個念頭會是「統一成一種」，而那會把對的那一處也改掉 |
| 65 | **Organization 的 `description` 改成 per-locale，法人名與地址不動** | `_identity.html` 是每一頁都 include 的共用節點，它那句中文 `description` 因此出現在 20 個英文頁與日韓頁的 JSON-LD 裡——**一個宣告 `inLanguage: en-US` 的頁面帶著中文實體描述**（issue #241）。這與決策 #61 剛修掉的形狀相同（共用 WebSite 節點寫死 `zh-Hant-TW`），也與這個站在 locale 上一路的選擇一致（#55 每 locale 一組 CJK 堆疊、#56 `heading structure` 條件化、機關名各語言各自寫）。**分界寫在這裡**：`description` 是**關於**實體的散文，隨讀者的語言走；`legalName`、地址、`taxID`、`name` 裡的登記名稱是**實體本身**，五種語言同一份，`check:entity` 斷言它。做法是既有的 `[locale.strings]`（footer 的 `copyright` 同一個機制），五份各自用該語言寫而不是翻譯腔。⚠️ **兩個代價**：一、這些字串落在 JSON 裡而 minijinja 會 HTML-escape，一個 `'` 會變成 JSON 字串裡的 `&#x27;`——合法 JSON、錯的文字、而**沒有一道閘門讀散文**，所以字串本身要避開被 escape 的字元，理由寫在 `site.toml` 那格旁邊。二、`check:i18n` 讀模板不讀 `dist/` 的理由是「每頁都帶著那六個中文標點」，而那六個全在這句 description 裡——**理由過期了，閘門沒改**，記在 NOTES.md |
| 66 | **韓文引號用 ledger 守，延伸既有那道閘門而不是開第二套** | PR #237 逐處判斷了 301 處引號（直接引述 `""`、術語 `''`、法規與條目名 `「」`），**而那 301 個決定沒有任何東西守著**（issue #240）：下一個人加一頁韓文或改一段，引號用錯不會有人知道。判準需要語意，寫不成規則——這與詞間空白是同一個形狀，所以用同一個答案：`scripts/check-ko.js` 記錄每一處引號連同它用的是哪一對，只在改變或新增時說話，**不判斷哪裡該用哪一種**。實作上的三個選擇：**兩份 ledger 並排**（`ko-spacing.txt` 777 處、`ko-quotes.txt` 325 處）而不是混在一份，因為空白那份的 `··`／兩格前綴對引號沒有意義，而兩份各自的 header 才說得出真話；**`&ldquo;` 與 `“”` 記成與 `""` 不同的一類**，它們是同一個決定的第二種拼法，分開記是為了讓拼法改變看得見而不是被抹平。⚠️ **它本身不是錯**——今天那 2 處是首頁的引言，而五個 locale 的首頁都用 `&ldquo;` 寫；ledger 把差別顯示出來，然後對它不發表意見。這一格原本寫「讓不一致出現在 ledger 上」，那是在還沒去看其他四個 locale 之前寫的，把一個站台慣例說成了缺陷；**閘門改名 `check:ko-spacing` → `check:ko`**，因為一個叫 spacing 的閘門同時守引號，就是這份文件數過三次錯的那個形狀，而拿掉範圍字也讓下一類進來時不必再改一次。⚠️ **它守的仍然只是「有沒有變」**，不是「對不對」——325 處的正確性整批繼承自 PR #237 與 #242 的人工判斷 |
| 67 | **首頁是主張，不是門口** | 首頁的內容段落移除之後，它剩一句主張與一個控制項。兩種讀法會導出相反的修法：門口的話頁尾的連結是第二個入口、hero 越短越好；主張的話 hero 必須是頁面上最重的東西。選主張，因為刪掉那五段的動作本身就說了「不要首頁當目錄」，而 hero 裡 τ／時間常數那段是全站唯一講品牌來歷的地方。**代價**：頁尾佔比與 CTA 位置都因此變成可以失敗的條件，而不是品味問題 |
| 68 | **驗收條件是「CTA 在第一屏」，不是「hero 比頁尾高」** | 「hero 要最重」得變成數字才進得了閘門。hero 比頁尾高在 375px 上幾乎必然失敗（18 條連結疊起來）；「hero 佔滿第一屏」當時就已經通過，咬不住任何東西。CTA 在摺線上是三者中唯一**既是真缺陷又修得動**的：量出來它離摺線只有 11px。**代價**：這條門檻只在 375×812 以上成立，見決策 #69 |
| 69 | **做不到的尺寸就寫下來，不硬擠** | `fold` 探針只跑 375×812、390×844、412×915。375×667（SE 2022）與 320×568（SE 2016）實測差 15–67px，日文最差。把它們擠進去要把 hero 上下留白從 112px 壓到 32px——**用讓主張讀不下去，去換按鈕在螢幕上的位置**，與決策 #67 直接矛盾。那一版做出來、量完、退回。**代價**：兩個尺寸上首屏看不到 CTA，而這件事寫在探針註解裡而不是假裝不存在 |
| 70 | **接近性有比例了，而它是量出來的不是訂出來的** | 群組邊界後的留白三階：cover 的 96px、頂層 `pt-16`（64px）、巢狀 `pt-12`（48px）；組內是 24／16，所以組間比組內約 **2.7:1**。⚠️ **這條規則不是在修「七種隨機留白」**——那個說法來自把 `<p>`、`<li>`、`<div>` 上的分隔線也算進去的 grep，而列表列之間的線和兩個段落之間的線是不同的工作。只看 `<section>`，本站原本就已經 355/375 一致。真正的缺陷是一個巢狀 section 用 `pt-8`，以及五份同時帶兩次 `border-t border-line` 和兩個 `pt` 的 section |
| 71 | **表面只在自己是表面、或被抬離表面時才畫** | `body` 畫著唯一的表面，所以裡面任何 `bg-surface` 都是白上加白——373 處。⚠️ **移掉 `bg-surface` 並不會把那些形狀修好，第一版就停在那裡**——code review 指出 `list-none` 清單裡那 40 個 span 變成 `w-1.5 h-1.5 rounded-full mr-3`，仍然畫不出任何東西，而規則卻在它被寫來抓的那個 bug 上報綠。照本決策的「刪除而非復活」，那 40 個空 span 整個刪掉，另外 120 個沒有填色也沒有邊框的 `rounded-full` 一併移除（**半徑畫在什麼都沒有的東西上**）。有填色或邊框的 275 個原封不動。⚠️ **原本提的規則「後代一律不准」是錯的**：量完發現語系切換器的下拉是 `absolute`、浮在導覽列上，它需要那個底。判準因此是**位置**（`body`，或帶 `absolute`／`fixed`）而不是名單 |
| 72 | **視窗相對單位必須在載入時就是那個尺寸的視窗上量** | walk 的經濟性是「resize 只花一次 reflow」，對其他探針成立，對 `.band` 不成立——它用 `min-height: 100svh`，從 900 縮到 812 之後仍以 900 計算，`justify-content: center` 把 hero 往下推 (900−812)/2 = **44px**。五個語系在走訪下都剛好差 44px。`fold` 因此是唯一帶 `reload: true` 的探針。⚠️ **而 `reload` 在探針本體之前就執行**，所以第一版付了 101 條路由 × 3 個視窗的導覽費去換 15 個量測——探針在頁面內提早 return 省不到任何東西。walk 因此多了一個 `probe.paths`，讓探針**向走訪宣告**它關心哪些路由。**代價**：實測多約 40 秒 |
| 73 | **行長那張表量的是樣式表，不是排版** | 五個字系的上限全部收斂成 68，因為 `:where(main p, main li) { max-width: 68ch }` 是無條件的，實測 2818 個元素**沒有一個** cap 更寬。`Latn: 75` 打不到——最寬的英文段落 68.11ch，要到 75 需要 566px 而樣式表只給 514px。⚠️ **這根探針守的是那條 CSS 規則沒被拿掉，不是「沒有行太長」**；ja-JP 與 en-US 的舒適行長至今沒有被量過，因為這個站從來不讓段落寬到足以回答 |

---
### v5 新增（mono-color）

| # | 決策 | 依據 / 代價 |
|---|---|---|
| 74 | 參考站換成 `yanliudesign/mono-color-skill` | **第三次品牌重置**，三個參考站兩兩矛盾。**代價：這一個不是網站，是印刷品規範**——它不談斷點、hover、focus、圓角、locale，所以「本站推導」這次撐住半份文件，而不是補幾個洞。**收穫是一條新的可檢查規則**（墨版模型），這是三次重置裡第一次拿回規則而不只是換值 |
| 75 | 表面 `#ffffff` → `#FAFAF7` Neutral White | 參考站的三種紙**沒有一種是純白**，而且明文要求 keep the paper visible。**不選 Cool Gray `#E9E9E5`（字面上最貼「technology / restrained branding」）是算出來的**：舊次階 `#707070` 在它上面是 4.07，低於 AA 4.5；Pale Beige 是 4.39，也不過。只有 Neutral White 的 4.74 過。**代價：所有算過的對比全部要重算，而且要與階段 ① 一起改** |
| 76 | **兩塊墨版是上限，深淺是密度階不是新顏色** | 參考站：never more than two ink plates；darker coverage may appear near-black — these are density changes, not extra inks。**這是 v5 唯一的結構性收穫**：v4 的五個顏色 token 每個都有理由，但沒有東西回答得了「可以有第六個嗎」。現在回答得了。⚠️ **當時留下的未決問題「alpha 語法算不算合法密度階」已由決策 #105 回答：算** |
| 77 | 主墨版 Charcoal `#30343A`，三階改為覆蓋率並改名 | 參考站列 Charcoal 給 research／restrained publications。三階算過：100% `#30343A` 11.97、86% `#4C5054` 7.78、72% `#696B6F` 5.11。**72% 是唯一沒得選的一階**——64% 是 4.06，不到 AA。**名字從 `-80`/`-48`（不透明度）改成 `-86`/`-72`（覆蓋率），因為兩套數字不同義**；沿用會產生一個叫 `ink-80` 卻是 86% 的 token。**代價：改名會動到每一個使用者，而 `check:classes` 抓得到未知類別、抓不到用錯階** |
| 78 | Action Blue `#0066cc` → Cobalt `#2148B8` | 參考站的 one-ink 預設，明文給 technology／knowledge。7.48:1，比舊值 5.57 高。角色不變：**承載所有互動，不是裝飾**。本站第四次面對「要不要第二個強調色」，而這次終於有規則（26）而不只是一句話 |
| 79 | `primary-focus` `#0071e3` 移除 | 兩塊版的上限下，第三個藍是第三塊版。focus ring 改用 `primary` 本身，靠**環寬與 offset** 區分。**代價：focus ring 與連結同色，鍵盤使用者要靠形狀分辨聚焦。** 這是 v5 讓可用性變差的唯一一處，寫下來而不是留給下一個人發現 |
| 80 | `on-primary` 改為由 `canvas` 推導 | **從墨版挖空看到的是紙。** 值仍是 `#FAFAF7`，但來源變了。⚠️ **這推翻 v4 的理由**——v4 說兩者要分開命名，好讓「表面變了」與「藍底上的字變了」不是同一次改動；在墨版模型下**它們就是同一件事**。名字保留（改不划算），但**兩個值不相等的時候那是 bug 不是設計** |
| 81 | `hairline` `#e0e0e0` → `#E2E2E0`（主墨版 12%） | 參考站：thin one-ink rules，線由墨版畫。新舊值只差 2/2/0，**但來源不同：一個是挑的，一個是算的**。⚠️ **未驗證：1.24:1 會不會被 `contrast` 閘門當成文字讀而在階段 ① 紅掉。改之前先驗** |
| 82 | `display-lg` 56px → **64px** | 參考站：largest text is 5-12 times the microcopy size。**56/12 = 4.67×，不到 5——這是本站唯一沒過參考站既有規則的值。** 60 是 5.00 剛好壓線，64 是 5.33 有餘裕且為整數 rem。⚠️ **分母取 `eyebrow` 12px；用 `caption` 14px 算是 4.57，一樣不過，兩個都算過了** |
| 83 | 排版角色只取 `type_programmatic` 一張，不取整副牌 | 參考站要的是**會隨內容換 display 骨架的字體卡司**。**網站做不到**：換 display 面只有自架多套字體（決策 #4 已否決）或依頁面切系統字（同一個標題在 100 條路由上長得不一樣）。選這張卡的理由是本站的事實：15 頁 research 長文、五個 locale 的 bilingual-safe spacing、SF Pro 本身就是 engineered modular sans。**代價：放棄參考站在排版上最主要的主張** |
| 84 | support voice 取 tabular numerals，不取 mono | `type_programmatic` 同一張卡列了三個 support 選項，`zero mono`（規則 13）守著其中一個不能用。**這不是繞過參考站，是它自己列了三個** |
| 85 | 卡片網格：全面禁止不採用，縮小成 `no card wall` | 參考站 hard avoid 列 card grids。實測 zh-Hant-TW 有 `grid-cols-2` 9、`grid-cols-3` 7、`grid-cols-5` 2，共 18 處，**多數是資訊並排不是卡牆**。判準改成「每一格同時帶邊框＋圓角＋內距」。⚠️ **門檻寫不出來因為還沒量過，排階段 ③** |
| 86 | 外邊距 5–9% 與留白 25–55% **不採用為規則** | 邊距實測六個視窗**只有 1280px 落在區間**（5.0%）；768px 是 0%、1920px 是 20.0%。留白的分母（版面高度）在網頁上由內容決定，根本不存在。**海報的比例是版面的性質，網頁的是視窗的性質。** 兩條都改記在「沒有東西檢查的事」——**寫成規則會是永遠不紅的規則，那與沒有規則相同** |
| 87 | 字級名稱改對齊 `tailwind.config.js` | v4 的表寫 `hero-display 56 / display-lg 40 / display-md 34`，config 宣告 `display-lg 3.5rem / display-md 2.5rem / display-sm 2.125rem`。**逐項比對行高與字距完全相同**（1.07/−0.005、1.1/0、1.18/−0.011）——**同一把梯子，名字整排錯位一格**。v4 只抓到「`hero-display` 這個類別不存在」，沒抓到底下兩階也錯。**一個名字錯像筆誤，三個連續錯位是整欄下移。** 本檔改用 config 的名字，`hero-display` 這個名字退役 |
| 88 | 由本站推導的段落一律標〔推導〕 | 裁決規則是「參考站為準，沒涵蓋的才推導」，而 v5 的推導量大到讀者分不出哪些有參考站背書。**標記讓權威等級看得見**：〔推導〕的段落改起來只需要本站的理由，沒標的要回去對參考站。**代價：多一個要維護的標記，而沒有東西檢查它標得對不對** |
| 89 | **先宣告載體，再套規則** | 不採用表的理由幾乎全是「本站沒有那個物件」，**而那句話只在頁面上成立**。本站有三種載體：頁面（100 條路由，隨視窗）、OG 卡（100 張，1200×630）、圖示（8 個檔）。⚠️ **v4 沒有這一格，所以 100 張卡從來沒有被任何設計規則管過**——它們在 v4 只以「衍生資產」出現一次，講的還是建置問題 |
| 90 | 印刷規則的抽取判準：三關 | ①有對象嗎 ②分母存在嗎 ③量得到嗎。三關全過寫成規則，過兩關寫成注意事項，過一關留在不採用表。**「留白 25–55%」在頁面上第②關就死（版面高度由內容決定），在 OG 卡上三關全過（1200×630 是常數）。同一條規則換載體就活了**——這是判準本身的價值，不是某一條規則的價值 |
| 91 | **「主體佔版面」量墨跡外接矩形，不量深色像素比例** | ⚠️ **這一格是量錯之後才寫的。** 第一次用深色像素比例量 100 張卡，得到 86.0–97.1%（中位數 93.2%）與「0/100 通過」；換成墨跡外接矩形重量，得到 64.8–65.3% 與 **100/100 通過**。**純文字的卡不管字放多大，深色像素都到不了 45%**——文字大部分是字腔與行間。**兩個數字都是量出來的，其中一個是錯的。量對東西跟量這件事一樣重要**，而本檔開場那三段自白講的都是「沒有量」，這是第一次講「量錯」 |
| 92 | OG 卡的 kicker 17 → 12px ✅ 已落地 | 卡是本站唯一三關全過的載體，實測**四項符合、一項不符合**：級距 `80/17 = 4.71×`、長標題分支 `62/17 = 3.65×`，都不到 5。另一條修法（標題升到 ≥85px、砍掉分支）**排不下**——85 字的標題約 5 行，扣掉上下邊距只剩 473px，還要放 kicker、mark、細線與描述。改 kicker 之後是 6.67× 與 5.17×，而且**它就變成頁面上已經有的 `eyebrow` 那一階**。⚠️ **代價兩個：卡在動態消息裡縮到約 500px 寬時 12px 只剩約 5px；長標題分支的 5.17× 離下限只有 0.17，沒有餘裕** |
| 93 | 標題字級的分支保留，但記為缺陷 | `build-og.js:106` 依 `title.length > 34` 在 80/62px 之間切換，實測 **59/100 走 62px 分支**，而比例隨語言差很多（`en-US` 15/20、`zh-Hant-TW` 9/20）。**所以同一頁的卡，字級隨語言不同**——那不是設計決定，是標題字數（含站名後綴）的副作用。**分支沒有拿掉，因為拿掉排不下**；寫下來是為了讓下一個人知道它是已知缺陷不是設計 |
| 94 | 規則 31 `declared surfaces`：**檢查缺席** | 深色模式、`forced-colors`、`@media print`、`::selection`／`caret-color`／`accent-color`、`theme-color`——**五個位置全站都是 0 個宣告**。⚠️ **沒有宣告不等於沒有顏色，是用瀏覽器或作業系統的預設**，那是五個逃出兩塊版的表面。規則 25 掃樣式表裡出現過的顏色，**掃不到應該出現而沒有出現的**，所以這條的判準是清單不是模式。**本檔 32 條規則裡只有這一條在檢查該寫的有沒有寫** |
| 95 | 深色模式：**不做**，而且要明文宣告 | 墨版模型的答案是紙就是紙，反轉會讓 `#FAFAF7` 變成一個沒有名字的深色表面。⚠️ **但 `static/taux-logo-light.png` 與 `taux-logo-dark.png` 兩個檔案都在，而 `templates/`、`src/`、`generator/` 裡 0 個使用者**——兩個 logo 變體暗示兩個表面，而本站只有一個。**留著的死檔案會讓下一個人以為深色模式是待辦** |
| 96 | 規則 32：alpha 到達的密度階要有名字 | 實測 2 個實例：`bg-ink/5` 與 `::-webkit-scrollbar-thumb:hover` 的 `rgb(var(--ink-rgb) / 0.35)`，**兩個都沒有名字**。⚠️ **這條依賴規則 25 的未決問題**：alpha 階算不算合法密度階。答案是「算」的話這條是配套（要具名），是「不算」的話這條變成禁止。**兩個實例都要改，只是改法不同** |
| 110 | `display-lg` `3.5rem` → `4rem`，而規則 27 讀刻度不讀斷點 | 參考站要最大字是微字的 5–12 倍，`3.5 / 0.75 = 4.67×` 不到下限。**`3.75rem` 剛好 5.00×，壓在線上**，取 `4rem`（5.33×）。⚠️ **規則讀 `tailwind.config.js` 的刻度，不讀任何斷點量到的值**：`.display-lead` 是 `text-4xl tablet:text-display-lg`，手機上實際畫出來的最大字是 Tailwind 預設的 36px（3.0×），照斷點寫會為一個刻度從未做過的決定紅掉。⚠️ **比值不換算 px**——每一階都是 rem，而本站的根字級曾被膨脹到 17px，換算過的規則會跟著靜靜移動。實測改動確實到得了使用者：`375px` 仍是 36px，`768px` 與 `1280px` 是 64px。`geometry` 808 個組合 0 失敗，沒有溢位 |
| 109 | ⚠️ **副墨版那段散文三分之二是假的，而它跨了兩次品牌重置** | 文件從 v4 起寫「Cobalt 承載所有互動：連結、CTA、focus ring」，實測**只有 CTA 成立**：`a` 的顏色宣告 **0 個**（連結繼承 ink、靠底線標記，160 個 `<a>` 裡 112 個連底線 class 都沒有，其餘由元件規則 `@apply`），focus ring 是 `outline: 2px solid var(--ink)`（**11.97:1，比 Cobalt 的 7.48 更高**）。全站 Cobalt 只有 `.btn` 底色與 15 個 `<button>` 的 `text-primary`。**改文件不改程式**：底線標記連結是 WCAG 1.4.1 要的（資訊不得只靠顏色），ink 的 focus ring 對比更高——**程式做得比文件說的對**。⚠️ 連帶作廢決策 #79 的代價欄：它寫「focus ring 與連結同色，鍵盤使用者要靠形狀分辨聚焦」並標為「v5 讓可用性變差的唯一一處」，**而那個代價從未發生，因為前提是假的**；`--primary-focus` 實測**零使用者**。**寫下一個沒發生的代價，比不寫代價更糟——它讓下一個人以為那個權衡做過了。** 這三件全部是規則 26 把散文變成閘門的那一刻才浮出來的：**一句沒有東西讀的規則，跟一句錯的規則，從外面看一樣** |
| 105 | **alpha 算合法密度階**，而規則 25 與 32 是同一個答案的兩半 | (a) 參考站自己就是這樣定義密度的；(b) **`--ink-rgb / 0.12` 算繪後是 `#E2E2E0`，與 `hairline` token 完全相同**——同一階兩條路到達；(c) 禁止的代價是 186 個呼叫點。所以 **25 問「這是哪一塊版」、32 問「這一階在刻度上嗎」**。共用的解析器住在 `scripts/plates.js`：先把 `var()` 展開、疊到紙上算出**算繪後**的顏色，再反解 `C = a·P + (1−a)·S`。**反解是關鍵**——具名的 `#4C5054` 與 alpha 的 `/0.86` 都落在 ink@86.1%，實測三個通道分歧 < 0.005 |
| 106 | 密度刻度是 `{0, .05, .12, .2, .35, .72, .86, 1}`，而 `0.06` 併進 `0.05` | 八階全部量自真的顏色。`0.06` 是 `.btn-quiet:hover` 的單一使用者，在紙上是 `#EEEEEC` 對 `0.05` 的 `#F0F0EE`——**差 2 級，肉眼分不出**。**一把梯子上有一階分不出它的鄰居，就是決策 #52 的論點從反方向到達**，所以併進有 185 個呼叫點的那一階。安靜按鈕的階梯仍然成立：hover `0.05`、active `0.12`，對上大聲版的 `0.12` 與 `0.2` |
| 107 | ⚠️ **容忍值訂寬了 12 倍，讓規則 32 第一次跑就是綠的** | `COVERAGE_TOLERANCE` 憑感覺訂 `0.01`，而 `0.05` 與 `0.06` 正好相距 `0.01`——**閘門把那個不在刻度上的 `0.06` 吸附到 `0.05`，回報乾淨**。實測量化誤差最大只有 **0.0008**（`0.05` 解出 `0.0492`、`0.86` 解出 `0.8608`）。改成 `0.004`：是誤差的 5 倍，也在最窄的階距內 2.5 倍。**一條吸收得比它該吸收的多的規則，是一條會同意任何輸入的規則**——這個 repo 記過同一類（`build-og.js` 那支不可能觸發的字體斷言） |
| 108 | 規則 25 用注入證明它會紅，而不是相信它會 | 它今天本來就是綠的（樣式表每個顏色都反推得出來），而**一條沒被看過紅燈的規則，與一條不可能紅的規則從外面看一樣**。兩條路各注入一次：作者 CSS 的 `#C65F38`（Terracotta）→ 紅；模板 class 的 `bg-red-500` → 紅。**第二條是查出來才知道存在的**：`tailwind.config.js` 用 `extend`，而 extend 是加不是換，所以 Tailwind 自己的調色盤還編得出來（實際建置驗過）。**那條路不經過作者 CSS，只讀 `stylesheet.read()` 的話會整個漏掉** |
| 102 | 規則 1 加寬並改名，因為它的說明比它的實作大一個語法家族 | `ruleNoHex` 只比對 `#rrggbb`，而 summary 宣稱顏色屬於 token。⚠️ **實測 25 個字面色從縫隙走過**：`rgba(25,25,24,0.06)` ×10、`rgba(25,25,24,0.08)` ×5、`rgba(255,255,255,0.04)` ×5、`rgba(255,255,255,0.08)` ×5。**`25,25,24` 是 `#191918`，三次品牌重置裡沒有一次用過它**——那是第四套沒人宣告的顏色。**判準是括號後面第一個字元**：數字表示有人手寫了顏色，`var(` 表示他伸手拿了墨版，所以 `rgb(var(--ink-rgb) / 0.35)` 照樣綠 |
| 103 | 15 個洗色全部併到 `0.05`，不是保住原本的外觀 | 追求外觀一模一樣要鑄出 `0.07`（差 0）與 `0.09`（差 1）**兩個全新的未宣告階**，而那正是規則 32 要擋的債。改用站上既有的洗色 `0.05`（`bg-ink/5`，**185 個使用者**），而 `.highlight` 與那 185 個本來就是同一個角色。**代價：兩處各差 4 與 9 級，都在貼近紙的一端。** 保住舊外觀等於保住舊調色盤的值，而底色本來就從 `#191918` 換成 `#30343A` 了——精確保留 alpha 並不保留任何東西 |
| 104 | 那個 inline `style=""` 整個刪掉，而它有一個看得見的後果 | `<pre class="code-window">` 上掛著 `background-color: rgba(255,255,255,0.04); border-color: rgba(255,255,255,0.08); box-shadow: none;`。**三個宣告在紙上有兩個是空的**：白 4% 疊在紙上就是紙（離紙 0），而基底本來就是 `var(--surface)`；`box-shadow: none` 覆蓋的是一個不存在的陰影。**第三個不是空的**——基底是 `border: 1px solid var(--line)`，而白 8% 在紙上等於紙，所以那行的真正效果是**把邊框弄不見**。⚠️ **那在黑底時代是「深色上的淺邊框」，換成白底之後變成「沒有邊框」，而沒有人選過這件事**：兩個並排的 code window，一個有髮絲線一個沒有。刪掉整個屬性，第二個拿回它的邊框。**這是這次清理唯一看得見的改動** |
| 99 | 三個烤死舊表面的資產，兩個處理一個保留 | 拷問階段 ① 的時候查出來的，**都不是這次改動造成的，是現況就錯**。⚠️ `static/site.webmanifest` 的 `theme_color: #000000` 與 `background_color: #0a0a0b` 是**黑底時代殘留，兩次改版沒跟上**，而它線上生效（`templates/header.html:81` 掛著它，Android PWA 啟動畫面用它）——改成 `#fafaf7`。⚠️ `build-icons.js` 的 `BACKGROUND = "#000000"` 註解寫「`--surface-deep`. Matches the page the icon links to」，而 `--surface-deep` 不存在、頁面從 #54 起就不是黑的——**顏色保留，註解改寫**：favicon 不住在本站的表面上，它住在瀏覽器工具列與桌面，那些不是我們選的，紙印在紙上讀不出來。**保留的理由從「繼承來的值」換成「決定」**，這樣下次換表面不會再靜默地讓它錯。`brand/logo-on-light.png` 的白底不動——那是 Google 知識面板的表面，同樣不是我們選的 |
| 100 | `--primary-focus` 這張票只指向 `--primary`，幾何區分另開票 | 決策 #79 說移除並改靠**環寬與 offset** 區分，那是兩件事：拿掉一個顏色（符合兩塊版）與改變鍵盤使用者看到的東西（無障礙表面）。**混在 22 個 token 的改動裡一起過，等於讓一個 a11y 改動搭便車。** 這張票寫成 `--primary-focus-rgb: var(--primary-rgb)`，token 名字留著、顏色少一個；環寬與 offset 排階段 ③ |
| 101 | token 改名的爆炸半徑是量的，不是估的 | 決策 #77 說改名會動到「每一個使用者」，**實測不是**：`--ink-muted-80` 在 `src/input.css` 3 處、`tailwind.config.js` 1 處、**模板 0 處**。模板用的是 Tailwind 的角色名 `ink-muted` / `ink-soft`，那一層**不必改也不該改**——它們編碼的是角色不是數字，說謊的只有 custom property 的名字。**四個編輯，不是一場遷移。** 估「每一個使用者」而不去數，正是本檔開場反對的東西 |
| 98 | 卡的字級改成具名匯出，而不是留在模板字串裡 | 規則 30 要斷言的東西原本沒有對外介面。**regex 版本會漏掉標題**（寫成 `${cond ? long : short}px`），而且**模板一重排就靜默轉綠**——一條會在真的壞掉時說沒事的規則比沒有規則糟。`build-og.js` 現在匯出 `TYPE_SCALE`，CSS 由它產生。⚠️ **同一次改動把 `main()` 包進 `require.main === module`**：它先前無條件執行，`require` 這個檔會開 Chromium 重寫 100 張 PNG。**這與 `routes.js` 是同一個形狀**（規則 19「單一路由表」）：一份資料一個來源，其他東西讀它 |
| 97 | `@media print` 是缺口，而且是最諷刺的那個 | 參考站是**印刷規範**，而本站按 Cmd+P 印出來的東西**不受這份文件管**（`@media print` 全站 0 個）。**這是唯一一個參考站的主場，而本站在那裡是空的。** 沒有立刻補，因為列印樣式要先決定印什麼（導覽、footer、locale 切換器要不要印），那是內容決定不是顏色決定 |

---
## 附註：這一版尚未涵蓋

**參考站給了、本站沒有對象的：**

- **網點與照片**：參考站的核心主張，本站 `<img>` 實測 **0 個**。**第三個參考站，第三次核心主張是空的。**
- **主體佔版 45–80%、標題與主體碰撞、紙張穿過影像**：都以照片為前提。
- **印刷缺陷**（`imperfections.json`）：套印偏移在網頁上是渲染錯誤，不是質感。
- **九種版面家族**、**七種載體**（`carriers.json`）：沒有一種是網站。
- **3:4 直式版面**：網頁沒有固定比例。
- **一次手繪介入**：本站的機械系統是 CI 規則不是版面，手繪符號在這裡會變成沒人管的裝飾類別。

**參考站沒給、由本站推導的：**

- 斷點、hover／active／focus、圓角、五個 locale、元件、動態、CJK 字體堆疊。全部標〔推導〕（決策 #88）。

**跨版本沿用的：**

- CJK webfont：**不引入**（決策 #4）。
- 暗磚交替：不採用，全站單一表面。
- shop 與 configurator：不採用。

**v5 今天只改了這份文件。** 階段 ①–③ 的落地見「遷移狀態」，而**階段 ① 不可分割**——表面、三階墨色、`hairline`、`on-primary` 的值互相定義，拆開改會在中途讓 `contrast` 閘門紅。

**還沒量過、寫成待辦不是結論的三件事：**

- **頁面 `<h1>` 領銜句的詞數**。參考站要 display copy 2–8 個詞，本站只量了 OG 卡的標題字數（中位數 36 字元），沒量頁面。
- **`no card wall` 的門檻**。18 處網格裡有幾處會紅，沒量過，所以規則寫不出來。
- **`hairline` 1.24:1 會不會被 `contrast` 閘門當文字讀**。階段 ① 改之前要先驗。

**OG 卡上有四個字級**（mark 26、kicker 17、desc 23、title 62–80），而參考站要一個 display 聲音＋一個支援聲音。26 與 23 是不是同一個聲音沒有被決定過——**這一格今天是開的**。
