# TauX 設計規範

這份文件是**設計的唯一來源**。顏色、字體、字距、圓角、間距、元件與動態，全部在這裡決定；`NOTES.md` 不再描述設計，`src/input.css` 與 `tailwind.config.js` 的註解只解釋實作細節，不定義規則。

**規則寫了沒有東西檢查，就等於沒有規則**，所以這份文件的每一條都盡量寫成 `npm run check:design` 檢查得到的形狀。

> **這份文件自己也犯過同一個毛病。** 初稿宣稱 `adk-skill-patterns.html` 有 20 個 hex 值，那是把 `&#123;` / `&#125;`（為了 minijinja 而轉義的大括號）誤讀成顏色。實際數字是 **0**，全站皆然。留著這條錯誤的代價不是筆誤而已：照字面寫出來的檢查器第一次執行就會送出 20 個假警報，而一支開場就誤報的檢查會教會所有人跳過它。目視得出來的數字也要用工具數過——這正是這份文件存在的理由。
>
> **v2 也犯了一次，而且更嚴重。** 提案初稿在同一張表裡同時寫下「eyebrow → Roboto Mono」與「所有中文 → 系統中文字（硬限制）」，而實測 186 個 eyebrow 裡有 **95 個含中文**——兩句話不可能同時成立。更糟的是為它寫的檢查規則會漏掉全部 95 個，因為 `.eyebrow` 是元件類別而規則檢查的是 class 字串。落地前的逐檔清點抓到了它，解法見「等寬字治理」。同一份文件也把 canvas 數量寫成 16／12（實際 24／15）、分享卡寫成 20（實際 18）、模板數同時寫成 21 和 23（實際 23）。**這些都是「照文件實作會做錯事」的數字**，不是筆誤。

---

## 這套語彙的來源

參考站是 **`jules.google`**，取代先前的 `www.spacex.com`。

**只取行銷層。** Jules 有三個表面，而它們是**三套不同的設計語言**——弄錯會直接抄錯對象：

| 表面 | 語域 | 字體 | 底色 |
|---|---|---|---|
| `jules.google`（行銷） | 復古／8bit／ASCII | Roboto Mono，**全站** | 單一寫死深紫，**無淺色模式** |
| `jules.google.com`（產品） | 標準現代 Google 產品 | Google Sans | 明暗可切換 |
| `jules.google/docs`（文件） | Astro Starlight | Google Sans Text | 深色可切換 |

**本站只以行銷層為參考，明確排除產品層與文件站。**

### 裁決規則——與 SpaceX 時期相反

SpaceX 時期訂的是「參考站與本站規則衝突時，參考站為準」。**現在反轉：衝突時本站現行規則為準，參考站只在本站尚無規則之處補位。**

反轉的理由是兩次換參考站的目的不同。導入 SpaceX 是為了**建立**一套語彙——本站當時沒有可辯護的規則，讓參考站全面接管是對的。導入 Jules 是為了**補上技術性格**，不是把已經驗證過、有檢查器背書的骨架推倒。若照舊採「參考站為準」，深紫底、圓角、多彩、吉祥物會全數翻盤，那不是改版而是換一個品牌。

繁體中文與長文閱讀頁的既有例外照舊，標為「本站推導」。

### 參考站關鍵實測值

從 `jules.google` 的 HTML、編譯後 CSS、Preact bundle 與精靈圖的位元組層級檢查取得：

| 項目 | 實測值 |
|---|---|
| 頁面底色 | 單一寫死值，**無 `prefers-color-scheme` 區塊、無切換器** |
| 內文字體 | `Roboto Mono Variable`，**全站不載入任何無襯線字** |
| 字級刻度 | 只有四階（xs / sm / base 15px / lg） |
| 層級機制 | 靠**字重與顏色**，不靠字級。字重只用 500 / 700 / 900 |
| 字距 | 編譯後 CSS 裡**沒有任何字距 utility**——從不做視覺緊排 |
| 強調機制 | 關鍵字加 3px 有色底線，像螢光筆 |
| 圓角 | 階梯狀 `clip-path`，10px／20px 級距——由方形像素構成的轉角 |
| 陰影 | **`box-shadow` 零次**。深度靠位移的複製區塊 |
| 邊框 | 卡片 4px，重且平 |
| 分隔線 | 不是線，是 8px 高的 dither 點陣帶 |
| 圖示 | 24×24 SVG，`shape-rendering="crispEdges"`，路徑座標落在整數格 |
| 精靈圖 | **真的 4-bit／2-bit／1-bit indexed PNG**，以 `image-rendering: pixelated` 放大 |
| 主視覺 | 畫在 canvas 上的 ASCII 字元網格 |
| 過場 | 基準 .15s，`cubic-bezier(.4,0,.2,1)`。**全站只有一個 easing** |
| 間距 | 基準 .25rem，節奏粗（gap 4／8／10／20），偏寬鬆 |

### 本站不採用的項目，以及理由

| 參考站做法 | 本站決定 | 理由 |
|---|---|---|
| 深紫底 + 洋紅／青／黃三色 | **不採用** | 本站保留純黑表面與單一墨色。三色組合是參考站可辨識的商業外觀 |
| 全站等寬 | **不採用** | 參考站沒有中文。系統中文字沒有等寬版本 |
| 無字距 utility | **不採用** | 本站的字距刻度承載品牌聲音，且已有檢查器守著 |
| 階梯狀 `clip-path` 圓角 | **不採用** | 本站是零圓角。且該做法與 4px 硬邊框、無陰影的組合是參考站的卡片語言 |
| 吉祥物、擬人角色 | **不採用** | 見「語域」 |
| ASCII 字標畫在 canvas 上 | **不採用** | 見決策 #26 |

### 語域——形式取行銷層，語氣取產品層

參考站行銷層的 8bit 語彙在做的是**去風險化**：把一個會自動改寫你 repo 的 agent，包裝成一隻無害的遊戲角色。那對它是對的，因為使用者點進產品後會看到一個嚴肅的工具，兩層互相校正。

**本站沒有第二層。** 官網就是全部，而它要賣的是專業判斷。專業判斷不需要去風險化，需要的是**精確感**。

因此：**取行銷層的形式，取產品層的語氣。**

明文排除：吉祥物、擬人角色、彈跳與呼吸動畫、感嘆號文案、以及任何暗示執行階段能力的裝飾元件（假的主控台、假的進度條、假的「思考中」狀態）。本站沒有 runtime code——`wrangler.jsonc` 沒有 `main`，部署的是靜態 bytes——做一個不會動的假主控台，來訪的技術讀者會直接看穿。

同樣的點陣語彙，拿掉角色之後，結果是**儀器面板**而不是遊戲。

---

## 遷移狀態

這份文件描述的是**目標**，不是全部的現況。落地分三階段（票 #125）。

| 章節 | 狀態 | 階段 |
|---|---|---|
| Token — phosphor 強調色 | ✅ | ① |
| 字體 — box-drawing 子集 | ✅ | ① |
| 字體 — Departure Mono | ✅ | ① |
| 版面 — 404 的 noindex 與 canonical | ✅ | ① |
| 字體 — mono 堆疊納入 CJK | ✅ | ② |
| 檢查 — 三條新規則（關閉進場） | ✅ | ① |
| 檢查 — phosphor budget 啟用 | ✅ | ② |
| 檢查 — 另外兩條規則啟用 | ✅ | ③ |
| 等寬字治理 — eyebrow | ✅ | ② |
| 等寬字治理 — 表格與數據 | ⬜ | **待補規範** |
| 點陣質地 — 分隔線與清單符號 | ⬜ | **待補規範** |
| 視覺元素 — τ 曲線 rasteriser | ✅ | ③ |
| 視覺元素 — parallax 位移取整 | ✅ | ③ |
| 版面 — 404 全頁點陣 | ✅ | ③ |
| 衍生資產 — 分享卡重生 | ✅ | ③ |

每完成一個章節就把該列改成 ✅，並在同一個 commit 裡更新。**不要讓這張表落後於程式碼**——一份過時的規範比沒有規範更糟，讀它的人沒有理由懷疑它。

---

## Token

全部定義在 `src/input.css` 的 `:root`。`tailwind.config.js` 只暴露語意名稱，不放值。**模板裡不得出現任何 hex 值**（`check:design` 檢查此項）。

### 表面

| 變數 | 值 | 用途 |
|---|---|---|
| `--surface-deep-rgb` | `0 0 0` | 全幅 hero 與滿版 section |
| `--surface-rgb` | `10 10 11` | 頁面基底 |
| `--surface-raised-rgb` | `15 15 17` | 需要層次的面板 |

基底比純黑亮一階，因為密集的中文筆劃壓在 `#000` 上會產生光暈溢散。

### 墨色

**一個墨色，層級靠 alpha 表達。**

```css
--ink-rgb: 240 240 250;
```

| 語意名稱 | alpha | 黑底合成值 | 用途 |
|---|---|---|---|
| `text-ink` | 1.0 | `#F0F0FA` | 標題、強調、按鈕標籤、hover 態 |
| `text-ink-body` | 0.8 | ≈ `#C0C0C8` | **內文**、eyebrow、日期、註解 |
| `text-ink-muted` | 0.9 | ≈ `#D8D8E1` | 導覽靜止態、次級標題、靜止連結 |

0.8 與 0.9 的用途看起來是反的——**不是筆誤**，見決策 #3。

### 髮絲線

```css
--line-rgb: 240 240 250;
--line:        rgb(var(--line-rgb) / 0.08);   /* 分隔線 */
--line-strong: rgb(var(--line-rgb) / 0.35);   /* 控制項邊框 */
```

只有兩個權重。更多權重就是髮絲線退回成裝飾的方式，不要加第三個。

### 圓角

```css
--radius: 0px;            /* 版面、區塊、圖片、分隔 */
--radius-control: 4px;    /* 互動控制項 */
```

**圓角只出現在摸得到的東西上。** `tailwind.config.js` 的 `borderRadius` 把 `sm`/`md`/`lg`/`xl`/`2xl`/`3xl` 全部指向 `--radius`，`full` 保留給正圓。**那些用不到的階不能刪**（決策 #10）。

### phosphor — 唯一的非墨色

```css
--phosphor-rgb: 255 176 0;   /* #FFB000 */
```

| 項目 | 值 |
|---|---|
| 在 `--surface` 上的對比度 | **實測 10.8:1** |
| 在 `--surface-deep` 上 | 11.5:1 |
| 在 `--surface-raised` 上 | 10.5:1 |
| AA 門檻（一般文字） | 4.5:1 |

三個候選都遠超門檻，差別在色溫與語意：綠 `#33FF33`（14.6:1）語意陳腔濫調且在台灣商務語境有政治聯想；青 `#00D3F2`（11:1）與冷白墨色區隔度低且接近參考站三色之一。**琥珀是唯一與冷白墨色色溫互補的**——兩個冷色並置會顯得髒。它同時是 P3 磷光體的真實色，與 8bit 語彙同源。

**使用上限（`check:design` 規則「phosphor budget」）：**
- 每頁 phosphor 元素數 **≤ 5**。規則解析 `{% include %}` 後對**渲染後的頁面**計數，所以 footer 裡的一個元素算在每一頁上。
- **不得用於內文段落**。
- 允許：數據關鍵值、當前狀態、ASCII 圖形的單一強調筆畫、`[aria-current]` 態。

⚠️ 實測階梯：`/100` 10.80:1、`/70` 5.63:1、`/65` 4.98:1、`/60` **4.38:1**、`/50` **3.35:1**。**`/65` 是文字還站得住的最低一階，不是 `/60`。** 對比稽核會抓到，但不要當成可以往下探的許可。`bg-phosphor` 配繼承的墨色文字是 1.62:1，那是一條看不見的色帶。

⚠️ **token 值可以改，名稱不可以改。** 改名的波及範圍實測為 **1,525 處** utility 出現位置、**20** 個模板檔案，外加三個以 `getComputedStyle` 或正則讀取這些變數的 JS 消費者（曲線繪製、圖表、分享卡產生器），其中只有分享卡產生器會**大聲失敗**，另外兩個會靜默降級成 fallback 值。

---

## 字體

**五支字體**，各有明確職務。

### D-DIN — 拉丁

標題與品牌聲音。窄體大寫是本站**最有辨識度的資產**。

### 系統中文

不引入 CJK webfont（決策 #4）。`unicode-range` 是保證中文不等待下載的機制，不是最佳化。

### Roboto Mono — 等寬

自架。承載**機器輸出**，見「等寬字治理」。

⚠️ **mono 堆疊裡的 CJK 字體是承重結構，而放進去是一個看得見的改動，不是 no-op。**

Roboto Mono 的 `unicode-range` 排除 CJK，所以等寬文字流裡的非拉丁字元會沿堆疊往下掉。CJK 字體放在**拉丁等寬 fallback 之後、通用 `monospace` 之前**，讓中文落在**與全站其餘部分相同的字體**上——這是 eyebrow 改等寬的前提。

⚠️ **順序不能反過來。** 把 CJK 排在 `ui-monospace` 之前，PingFang TC 會搶答那些不在 Roboto Mono 子集裡的符號（`→ ≥ ≈ ∞ ● ★`）並給出全形寬度，手工對齊的 ASCII 圖會散掉。見決策 #40。

受影響的元素比第一次清點的多：15 個帶 `font-mono` 且含非拉丁字元（含 `index.html` 用 `&tau;` 實體寫的第二個品牌 τ，第一次用字面掃描漏掉了），再加上**繼承** `font-mono` 的 `<pre>`／`<code>` 區塊——Tailwind preflight 給的，不帶 class，任何只找 class 的清點都看不到。

在正確的順序下，這些元素在 **Mac 上幾乎不變**（中文本來就落到 PingFang，符號本來就落到 SF Mono）；真正的改變在 **Windows**：中文從通用 `monospace`（細明體）變成微軟正黑體。那正是這次要修的東西。

拿掉那三個 CJK 字體，中文會落到通用 `monospace`，在 Windows 上是**細明體**——一款全站他處都沒用過的襯線字。**這個退化在 Mac 上完全看不見。** 見決策 #35。

### box-drawing 子集

| 項目 | 實測值 |
|---|---|
| 來源 | JetBrains Mono，OFL 1.1，**無 Reserved Font Name** |
| advance | **0.60000 em**（Roboto Mono 是 0.600098 em，差 **0.016%**） |
| 涵蓋 | `U+2500–257F` + `U+2580–259F`，160/160 |
| 縱向跨距 | **1.32 em**，四類字符一致（Roboto Mono 自身行框是 1.3189 em） |
| 檔案大小 | **2,544 bytes** |

宣告為 **`Roboto Mono` family 底下的第三個 `@font-face`**，帶 `unicode-range`。瀏覽器自動取字，混進任何等寬文字流，**任何字級都對齊，模板零改動**。

沒有它，這些字符會掉到系統 fallback，各平台字寬不同，ASCII 網格會散掉——**那是會壞掉，不是美感問題**。

### Departure Mono — 真點陣

| 項目 | 實測值 |
|---|---|
| 授權 | OFL 1.1，**無 Reserved Font Name** |
| 設計網格 | 7×11 px，upem 550，座標落在精確的 50 單位格線 |
| advance | **0.63636 em**，比 Roboto Mono 寬 **6%** |
| 檔案大小 | **4,140 bytes**（含拉丁） |

**6% 的差異代表它混不進任何文字流**——混進去會把每一欄剪歪。它也只在 **11px 的倍數**上像素完美。

因此它**只用於刻意框起、自帶固定字級與行高、不參與響應式型階的區塊**：404 頁，以及標記了 `data-specimen` 的樣本區。`check:design` 的「pixel face scope」規則守這條線。

### 授權義務

兩支新字型的子集依 OFL 1.1 第 2 條屬於 **Modified Version**（該條把「刪除元件」與「改變格式」都算進去）。授權檔隨檔案放在 `static/fonts/`，並保留上游版權行。**在 HTTP 上提供字型即構成散布，所以授權檔必須真的可取得**——`npm run contract` 會請求它們。

⚠️ **明確排除 DejaVu Sans Mono。** 它**不是** OFL、MIT 或 Apache，而是 Bitstream Vera + Arev + 部分公有領域，並帶有改名條件。把它寫成 OFL 會是與當年把 Roboto Mono 誤植成 Apache 同一類的錯誤。

### 字體堆疊

```
sans:      D-DIN, PingFang TC, Microsoft JhengHei, Noto Sans TC, system-ui, sans-serif
display:   D-DIN Condensed, D-DIN, PingFang TC, Microsoft JhengHei, Noto Sans TC, sans-serif
mono:      Roboto Mono, PingFang TC, Microsoft JhengHei, Noto Sans TC,
           ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace
pixel:     Departure Mono, monospace        ← 僅限 ASCII 區塊
```

---

## 字級與字距刻度

| Token | 字級 | line-height | letter-spacing | 字重 |
|---|---|---|---|---|
| `text-eyebrow` | 0.75rem | 1 | `0.09em` | 400 |
| `text-nav` | 0.8125rem | 1 | `0.09em` | 400 |
| `text-button` | 0.75rem | 1 | `0` | 400 |
| `text-base` | 1rem | **1.6** | `0` | — |
| `text-display-sm` | 2rem | 1 | `0` | 700 |
| `text-display-md` | 3rem | 1 | `0.02em` | 700 |
| `text-display-lg` | 3.75rem | **0.9** | `-0.017em` | 700 |

字距只有五個值：`0` / `0em` / `0.09em` / `0.02em` / `-0.017em`。`check:design` 檢查此項。

參考站行銷層**沒有任何字距 utility**，本站不採用這一點——刻度承載品牌聲音，且已有檢查器背書。這是「本站規則為準」的第一個具體套用。

內文 `line-height 1.6`（非 1.5）與 `max-w-prose 68ch`（非 75ch）是**本站推導**：CJK 排得比拉丁密。

---

## 標題治理

每個 `<h1>` 是**拉丁領銜 + 中文副標**，`check:design` 檢查 `display-lead` 與 `display-sub` 兩個字串的存在。

理由見決策 #6：**中文沒有大寫**，排版簽名只能由拉丁承載。這條規則在等寬字擴大之後更重要，不是更不重要。

`.display-sub` 在 `@apply` 之後**明確覆寫** `font-weight: 400; letter-spacing: 0; line-height: 1.15`——`0.9` 是拉丁專用，會裁到中文筆劃（決策 #5）。這是這份文件裡最容易在「順手整理一下」時被消滅的一條。

---

## 等寬字治理

等寬字的職務是**承載機器性質的資訊**。判準是一句話：**這段文字是不是被量出來或被列出來的？**

### 納入

| 內容類型 | 現況 | 目標 | 實測數量 |
|---|---|---|---|
| eyebrow 與小標籤 | D-DIN Condensed | **Roboto Mono** | 186 處／12 檔 |
| 表格儲存格 | 混合 | **Roboto Mono** | 210 個儲存格／10 張表／7 檔（57 個已是 mono） |

### 排除

| 內容類型 | 維持 | 理由 |
|---|---|---|
| 導覽列 | D-DIN Condensed | 品牌聲音 |
| 按鈕文字 | D-DIN Condensed | 品牌聲音 |
| 標題 | D-DIN Condensed | 品牌聲音 |

導覽與按鈕是**品牌聲音**，不是機器輸出。

### 中文怎麼辦——這一段是這章的核心

實測：**186 個 eyebrow 裡有 95 個含中文**（51%），**147 個非 mono 儲存格裡有 125 個含中文**（85%）。

提案初稿在同一張表裡同時要求「eyebrow 改等寬」與「所有中文不套等寬」，那兩句話不可能同時成立。**解法不是逐元素例外，而是堆疊本身**：

`font-mono` 的堆疊裡放了 CJK 字體，而 Roboto Mono 的 `unicode-range` 排除 CJK。所以同一條 CSS 規則之下，**拉丁拿 Roboto Mono、中文拿 PingFang TC**——這正是 `font-sans` 已經運作了很久的機制（D-DIN 同樣不含 CJK）。

不需要 `.eyebrow-latin` 變體，不需要 91 處手工套用，不需要一條規則。中文字本來就是方塊等寬，與 Roboto Mono 的拉丁混排對齊得很自然。

**代價寫在決策 #35**：守住這件事的只剩 `tailwind.config.js` 裡的一段註解。

### 表格 ⬜ 待補規範

10 張表、210 個儲存格，其中 57 個已經是 mono——那些正是真正的量測欄（`00`／`01`、`27 秒`、`+65%`），既有的慣例是對的。剩下 131 格多半是中文說明，套等寬只會更難讀。

**欄位標題（`th`）試過，退回了。** 三十個 `th` 都是短標籤，看起來是乾淨的一條規則，但落地時撞到兩件文件沒有處理的事：

1. **`th` 的瀏覽器預設字重是 bold。** 實測套上 `font-mono` 之後，欄位標題渲染成 **Roboto Mono 700**——而本站自架的 Roboto Mono **只有 400 一個字重**，所以那是**合成假粗體**。這正是字級 token 把字重寫死要防的事（決策：DIN 只出 400 與 700，把字重綁進 token 才不會有人用 `font-semibold` 合成一個字面上不存在的字重）。
2. **六個 `th` 已經帶著 `font-display` utility**，而 utilities 層勝過 base 層，所以基礎規則對它們無效。三十個裡只有二十四個會生效——比全做或全不做都糟。

要落地就得先決定：欄位標題的層級要用什麼承載？若照本文件既有的邏輯（層級靠字級、大寫與字距，不靠明度也不靠合成字重），答案大概是 `th` 比照 eyebrow——mono + uppercase + `0.09em`，字重回到 400。但那是一個**視覺決定**，不是實作細節，必須先寫進這裡。

---

## 點陣質地

### 分隔線與清單符號

⬜ **這一節目前沒有落地，因為規範本身還沒說清楚要用在哪裡。**

字型已經備妥（階段 ①），語彙也定義好了，但本文件同時存在兩句互相拉扯的話：本節說分隔線由 box-drawing 字符構成，而「髮絲線」一節說「只有兩個權重：一條分隔線、一道控制項邊緣」。站上現有的分隔線全部是髮絲線。

**要落地就得先決定**：box-drawing 是取代髮絲線分隔線，還是只用在特定情境（清單標記、樹狀結構、ASCII 區塊內部）？在那個決定寫進來之前，任何套用都是實作者在發明規範，而這正是本文件存在的目的所反對的。

以下是已經定好的語彙，供將來落地使用：

以 box-drawing 字符構成，**不是圖片、不是 CSS 漸層**：

```
├─ └─ │ ─ ┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼      製表符
█ ▓ ▒ ░ ▀ ▄ ▌ ▐                   區塊與網點
```

因為它們是**文字字符**，所以繼承 `currentColor`、參與文字流、跟著型階縮放、被螢幕閱讀器當文字處理（因此裝飾用途必須 `aria-hidden="true"`）。

### ⚠️ 點陣紋理不得用 `background-image`

**不得以 `background-image`（含 `repeating-linear-gradient`）套用在有文字後代的元素上。**

對比度檢查器在合成背景色時，若沿祖先鏈遇到任何 `background-image` 不為 `none` 的元素，會回傳「無法解析」並**跳過該元素**。

**那不會讓 CI 變紅，而是讓稽核變盲。** 一道變盲的檢查比一道不存在的檢查更危險，因為它還在報成功。決策 #15 早就用同一個理由拒絕了照片。

紋理走**字型字符**或 **inline SVG**。允許 `background-image` 的唯一情況是該元素及其所有後代都沒有文字節點——現況唯一的例子（`ai-smart-work.html` 的點陣底紋）正是這個形狀：漸層在文字的**兄弟**元素上，不是祖先。那是正確的先例。

`check:design` 的「texture not behind text」規則檢查此項。**它讀模板，所以 `input.css` 裡引入的 `background-image` 在它的視野之外**——這個限制是真的，記在決策 #32。

---

## 元件

現有元件（按鈕、`.btn-quiet`、導覽、eyebrow、標籤、引言、章節索引）的**幾何與行為全部不變**。只有字體變更：

| 元件 | 變更 |
|---|---|
| `.eyebrow` | `font-display` → `font-mono` |
| 表格儲存格（逐格判斷） | → `font-mono` |

按鈕與導覽**不動**。「內容區塊不畫外框」不變（決策 #12）——卡片是髮絲線輪廓，參考站的 4px 硬邊框不採用。

---

## 版面

**不變**，除了 404。

### 404

`/404` 是**唯一允許整頁使用 Departure Mono 的頁面**。它沒有正文語意、不需要響應式型階、不承載 SEO 價值。

它仍然必須保留 `display-lead` 與 `display-sub`——`check:design` 的標題結構規則同樣讀 `[[document]]`。

**已修正的缺陷**：`/404` 在靜態主機上回應 **HTTP 200**（`not_found_handling` 只管未匹配路徑），過去帶著自我指向的 canonical 且沒有 `noindex`，等於邀請爬蟲把「這頁不存在」當成一個頁面收錄。而唯一的 robots 規則擋的是 `/404.html`——一個只會轉址的路徑。現在 `canonical` 留空（同時抑制 `og:url`）、輸出 `noindex, follow`，而 **`robots.txt` 刻意不擋 `/404`**——連舊有的 `Disallow: /404.html` 也一併移除。

**meta 指令只有在爬蟲被允許抓取該頁時才會生效。** 擋掉抓取等於讓 `noindex` 永遠讀不到，而任何一條外部連結仍然能讓那個 URL 以「無摘要的裸連結」被列出——正是這次要消滅的結果。舊的 `Disallow: /404.html` 有同樣的缺陷，而且指向一個只會轉址的路徑。同時值得知道：`robots.txt` 裡 15 個具名爬蟲各自有自己的群組，依 RFC 9309 只服從最具體的那一組，所以 `*` 群組裡的 `Disallow` 對它們（含 Googlebot）本來就沒有作用。

`noindex` 同時存在於 `[[page]]` 與 `[[document]]`，兩者行為一致。它必須放在兩處，因為 serde 會**靜默丟棄**不認識的鍵：只有 `Document` 懂它的時候，寫在 `[[page]]` 上的 `noindex = true` 會乾淨地解析、渲染不出任何東西、所有 gate 全綠，而頁面照樣被索引。兩種條目在 `site.toml` 裡只隔兩個區塊，一個吃掉旗標一個不吃，是那種讀不出來的缺陷。

⚠️ **這個修正沒有任何 gate 覆蓋。** `contrast` 與 `contract` 都只走 `[[page]]` 路由，404 是 `[[document]]`，兩者從不請求它。驗證方式是手動：`npm run serve` 之後 `curl -i localhost:8099/404`。

---

## 動態

**捲動揭示動畫仍然全面禁止**（決策 #13）。`check:design` 硬性阻擋 `opacity-0` 與 `animate-stagger*`。

參考站行銷層有 `squid-bob`、`ink-appear`、`ping-visual` 等六組 keyframes，**本站一組都不採用**——它們全部服務於角色動畫，而角色已被語域規則排除。

過場時長與 easing 不變。

---

## 視覺元素

**不使用攝影**（決策 #15）。

### τ 曲線 rasteriser

三個既有圖形（τ 步階響應、極座標網格、干涉條紋）的**繪製數學完全不動**，只更換輸出層：從連續描邊改為點陣網格。

| 參數 | 值 |
|---|---|
| 網格單元 | **4–6px** |
| 涵蓋範圍 | **24 個 canvas，分佈 15 個模板** |
| 分享卡 | 18 張，經 `build-og.js` 注入曲線程式碼，**僅在手動重跑時跟著變** |

**不採用 8px 以上**：τ 曲線是**內容而非裝飾**（它是一個二階系統的步階響應，欠阻尼比 0.22），粗網格會讓那個資訊流失。

`prefers-reduced-motion` 下畫出**點陣化的最終靜止狀態**，維持既有紀律（決策 #17：穩定後改用計時器而非 rAF，不要放回去）。

**兩個實作上必須解決的問題：**

1. **曲線下方的填色**目前是 `createLinearGradient`。點陣網格沒有漸層這個東西——改為 **dither 密度漸變**（點越往下越疏）。dithering 這個技術本來就是為了在有限色階下表現漸層而發明的，用它取代漸層是語彙內的答案。
2. **全部 24 個 canvas 都掛著 `data-parallax`。** 硬網格做次像素位移會閃爍，這是連續描邊不會有的問題。`parallax.js` 的位移取整到**整數像素**即可消除——次像素重採樣正是成因。取整到網格倍數會讓視差變成一格一格跳，反而更明顯。

---

## 衍生資產

| 產生器 | 讀 token | 寫死 |
|---|---|---|
| `build-og.js` | ✅ 從 `input.css` 正則讀三個色值，缺失時拋錯 | **字級規格**（字距、字級、行高） |
| `build-icons.js` | ✗ | `#000000` |
| `build-logo.js` | ✗ | `#ffffff`（決策 #20） |

`build:og` 產生 **18** 張卡（`ROUTES.length === 18`）。`static/og/` 裡另有 `404.png` 與 `500.png` 兩張**孤兒**——來自 `78d705d`，沒有任何東西會重生它們，也沒有任何東西引用它們（`[[document]]` 的 `og_image` 寫死指向 `index.png`）。落地階段 ③ 一併刪除。

⚠️ **`build:og` 完全手動**——不在 CI、不在 `build:site`、不在 Cloudflare 建置指令裡。卡片是提交進去的二進位檔，**沒有任何東西比對它們**。字級規格一改，18 張卡立刻全部過期而且不會有人知道。

⚠️ **`.kicker` 是分享卡上的 eyebrow。** 把它改成等寬之前，必須先在 `fontCss` 陣列補上 `face("Roboto Mono", "RobotoMono.woff2", 400)`——目前只內嵌三支 D-DIN。少了這行，卡片會**靜默**退回通用無襯線字：review 時看不出來，但會永遠留在別人分享出去的連結上。

⚠️ 若 rasteriser 要讀 `--phosphor-rgb`，`tokenCss` 的擷取清單也要一併加，並在 `tau-curve.js` 給它與 `--ink-rgb` 相同的 `|| fallback` 形狀。

---

## 檢查

| # | 規則 | 狀態 | 由誰開啟 |
|---|---|---|---|
| 1 | 零 hex | ✅ | #50 |
| 2 | 禁捲動揭示 | ✅ | #50 |
| 3 | 字距刻度 | ✅ | #53 |
| 4 | 圓角只在控制項 | ✅ | #66 |
| 5 | 標題結構 | ✅ | #55 |
| 6 | 錨點完整性 | ✅ | #66 |
| 7 | phosphor budget | ⬜ | #125 階段 ② |
| 8 | texture not behind text | ⬜ | #125 階段 ③ |
| 9 | pixel face scope | ⬜ | #125 階段 ③ |

**尚不成立的規則以關閉狀態進場，並具名寫出負責開啟它的票號。** 規則物件的 `turnedOnBy` 欄位承載這件事；印出時有 `?? "no ticket recorded"` 的兜底，因為這條路徑先前是死的（沒有任何規則定義過該欄位，第一個關閉的規則會印出 `undefined`）。

### 其他 gate 在這次改動中的行為

| Gate | 行為 |
|---|---|
| `check:css` | 模板加了 class 卻沒重建 CSS 就會紅。最早、最吵、最有用 |
| `check:classes` | 讀建置後的 CSS。`text`/`border`/`bg`/`font` 前綴**本來就在清單裡**，新 token 不需要擴充它。真正會靜默跳過的是 `image-rendering-*`、`mask-*`、`accent-*` |
| `contrast` | 只走 `[[page]]`。**看不到 404、canvas 筆畫、邊框顏色、偽元素、SVG fill/stroke，以及沒有文字的 `bg-phosphor`** |
| `contract` | 會走建置後 CSS 裡的每一個 `url()` 並斷言 200——字型檔必須與 `@font-face` 同一個 commit。**同樣不請求 `/404`** |

⚠️ **`check:design` 會在整個落地過程中全綠**，因為既有六條規則與本次每一項改動都正交。這不是它壞掉，是它的範圍。別把它的綠燈當成「這次改動被檢查過」。

### 沒有東西檢查的事

- `?v=` 快取破壞查詢字串（`_headers` 的註解自己說了）。落地前它已經**落後五個版本**
- 18 張分享卡是否與現行 token 與字級一致
- mono 堆疊裡是否還留著 CJK 字體（決策 #35）

---

## 詞彙表

| 詞 | 定義 |
|---|---|
| **表面 / surface** | 三階背景，非純黑 |
| **墨色 / ink** | 單一前景色，層級靠 alpha |
| **髮絲線 / hairline** | 兩個權重的分隔與邊框 |
| **eyebrow** | 標題上方的小型大寫標籤 |
| **領銜句 / display-lead** | H1 的拉丁大寫首行 |
| **副標 / display-sub** | H1 的中文次行 |
| **phosphor** | 單一非墨色強調色 `#FFB000`。名稱取自 CRT 磷光體，提醒它的用途是「發光的狀態指示」而非「第二個內文色」 |
| **box-drawing 子集** | 掛在 `Roboto Mono` family 下的第三個 `@font-face`，只含製表符與區塊元素。模板不需要知道它存在 |
| **pixel（字體堆疊）** | Departure Mono。**不是**「等寬」的同義詞——與 `mono` 的 advance 差 6%，兩者不可混用 |
| **ASCII 區塊** | 刻意框起、自帶固定字級與行高、不參與響應式型階的區域。唯一允許使用 `pixel` 堆疊的地方，以 `data-specimen` 標記 |
| **機器輸出** | 等寬字治理的判準。指被量出來或被列出來的內容 |
| **語域** | 形式與語氣可以分開取用。本站取參考站行銷層的**形式**、產品層的**語氣** |

---

## 決策紀錄

每條規則背後的依據或代價。**改規則之前先讀對應那條**——下面每一項都是某次「順手改一下」造成的。

| # | 決策 | 依據 / 代價 |
|---|---|---|
| 1 | 設計規範集中在這一個檔案 | 先前散在 `NOTES.md`、`input.css`、`tailwind.config.js`，其中兩處已與現況不符且沒人發現 |
| 2 | 以 `www.spacex.com` 的實測值為準 | 「風格接近」無法檢查，數字可以。**已由 #23 取代** |
| 3 | 單一墨色 + alpha | 參考站實測只有 `#F0F0FA` 一色。內文放 0.8 而非 0.9，是為了維持與遷移前 `#c8c8cc` 同階的抗光暈亮度 |
| 4 | 中文走系統字體 | 繁中網頁字體切片後仍數百 KB 且 swap 重排。**Windows 的中文比 Mac 差一階，已知並接受** |
| 5 | `line-height 0.9` 只給拉丁 | 中文字身滿框，沒有 descender 空隙，0.9 會裁到筆劃 |
| 6 | 英文領銜 + 中文副標 | 中文沒有大寫，參考站的標題簽名只能由拉丁行承載。**代價：H1 的主要文字變成英文**，長文內部 H2 因此保留中文 |
| 7 | 小型大寫收到 `0.09em` | 實測值。遷移前的 `0.2em` / `0.25em` 約是參考站的兩倍 |
| 8 | 按鈕字距收成 `0` | 實測值。**代價：拉丁簽名的三個承載點少一個** |
| 9 | 圓角只給控制項 | 參考站實測（控制項 4px、縮圖 8px、社群鈕正圓）。遷移前的全域 0 比參考站更嚴格 |
| 10 | Tailwind 未用到的 `borderRadius` 階不能刪 | 刪掉後後來寫的 `rounded-md` 會落回 `0.375rem`，方角規則無聲破掉 |
| 11 | 自架 Roboto Mono | 等寬已在 7 個模板出現 33 次 + 24 個 code 區塊，卻交給系統堆疊——三台機器三種面貌，那樣不算設計的一部分 |
| 12 | 內容區塊不畫外框 | 「卡片是髮絲框」讓 14 條路由累積出 162 個全框方塊，其中 49 個是框中框 |
| 13 | 不用捲動揭示動畫 | 參考站實測 0 個。本站也有前科：IntersectionObserver 版本讓首頁 19 個元素永久隱形 |
| 14 | 移除進場逐層淡入 | 參考站沒有，且它讓 LCP 元素的可見時間晚 100–300ms |
| 15 | 不使用攝影 | 沒有火箭。加上 CSP `img-src 'self'`、對比稽核在背景圖上是瞎的、滿版影像會成為新的 LCP 元素 |
| 16 | 長文頁不切滿版屏 | 參考站沒有長文頁。切屏會同時毀掉連續閱讀與 `Ctrl+F` |
| 17 | τ 曲線呼吸用計時器不用 rAF | rAF 會讓主執行緒以螢幕更新率被喚醒卻什麼都不畫 |
| 18 | hero 曲線用遮罩淡出而非底板 | 這套語彙沒有底板；卡片是髮絲外框，永遠不是填色 |
| 19 | 規則配 CI 閘門 | 這個 repo 已經實證過靠 review 守不住：CSP 一年未生效、hex 規則寫了就破、55 個無效 class |
| 20 | `build-logo.js` 的白色不跟著改 | 結構化資料 logo 用在淺色底，與站內墨色無關。曾經有人把「深色底用的白色標記」誤當成「深色的 logo」放進 JSON-LD，Google 收到一張白底白字 |
| 21 | 章節索引的捲動高亮**不是**捲動揭示 | 決策 #13 禁的是「文字的可見性取決於腳本跑成功」。索引是十條真的錨點連結：腳本不在、載入失敗或拋錯，十條仍完整可讀、可點、可跳，失去的只有「知道自己在哪」。腳本只搬移 `aria-current`，樣式直接讀那個屬性——**一份狀態，不是兩份**。豁免以行為性質界定並寫在這裡，與 `#menuOverlay` 同一個理由：第二個豁免不可能在沒人開口的情況下被加進去 |
| 22 | `<main>` 用 `overflow-x-clip` 取代 `overflow-hidden` | `overflow: hidden` 會讓元素成為捲動容器，**其中的 `position: sticky` 因此全部失效**——實測捲 3000px 後 sticky 元素從 706px 移到 −2294px。`clip` 不建立捲動容器，保留水平裁切而不破壞 sticky |
| 23 | 換參考站至 `jules.google` 行銷層，並**反轉**裁決規則 | SpaceX 的任務（建立有紀律的骨架）已完成；導入 Jules 是為了補上技術性格。若照舊「參考站為準」，深紫底、圓角、多彩、吉祥物會全數翻盤，那是換品牌不是改版。**取代決策 #2** |
| 24 | 只取 Jules 行銷層，排除產品層與文件站 | 實測確認三個表面是三套設計語言。不指明會抄錯對象 |
| 25 | 取行銷層的形式、產品層的語氣 | 參考站的 8bit 在做去風險化，因為它的產品會自動改寫使用者的 repo。本站賣專業判斷，且沒有第二層可以校正語氣。**代價：放棄 8bit 最討喜的角色與玩心** |
| 26 | 不做 ASCII 字標，不做吉祥物 | 兩者都是參考站可辨識的商業外觀。本站已有 24 個 canvas 且要將其點陣化，**點陣化的 canvas 加上 ASCII 字標會非常靠近前者**。代價：放棄一個確實有效的視覺裝置。但視覺資產必須累積在拓思身上 |
| 27 | 加入 phosphor，本站第一個非墨色 | 點陣質地在嚴格單色下容易讀成「髒」而非「有質地」，需要一個維度區分結構與雜訊。**代價：「單一墨色」從此有一個例外，而例外會誘發第二個例外。上限五個元素就是防止那件事的閘門；若上限被鬆綁，這條必須重寫** |
| 28 | 選琥珀而非綠或青 | 唯一與冷白墨色色溫互補者。綠語意陳腔濫調且在台灣商務語境有政治聯想；青與冷白區隔度低且接近參考站三色之一。**代價：對比度 10.8:1 低於綠的 14.6:1，餘裕仍有 2.4 倍** |
| 29 | 等寬字擴大到機器輸出，但不碰導覽、按鈕與標題 | 判準是「這段文字是不是被量出來或列出來的」。導覽與按鈕是品牌聲音。**代價：站上同時存在兩種字體邏輯，作者每次都要判斷一次；判準寫成一句話就是為了降低這個成本** |
| 30 | box-drawing 用同 family 的第三個 `@font-face` | advance 差 0.016%，可透明混入任何等寬文字流，模板零改動。**代價：筆畫比 Roboto Mono 重約 37%。次選 Noto Sans Mono 筆畫更貼近，但縱向跨距只有 1.213 em，必須釘死行高——選擇不釘** |
| 31 | Departure Mono 只用於 ASCII 區塊，且鎖 11px 倍數 | advance 寬 6%，混入文字流會把每一欄剪歪；座標落在 50 單位格線，只有 11／22／33px 像素完美。**代價：這些區塊不參與響應式型階。這是真點陣字的本質，不是缺陷** |
| 32 | 點陣紋理不得用 `background-image` | 對比度檢查器遇到祖先帶 `background-image` 會**跳過**該元素——不會變紅，而是變盲。決策 #15 早就用同一個理由拒絕照片。**代價：某些紋理用 CSS 漸層更簡單，改用字型字符或 inline SVG 較費工。且規則讀模板，`input.css` 裡引入的漸層在它視野之外** |
| 33 | τ 曲線只換 rasteriser，不換數學也不換排程 | 一個渲染層的改動覆蓋 24 個 canvas 與 18 張分享卡，且不觸碰決策 #17。**代價：點陣化後精細度下降，網格限制 4–6px 就是為了讓「二階系統步階響應」這個資訊不流失** |
| 34 | 新規則以關閉狀態進場 | 本站既有慣例。規範裡的規則若沒有對應的程式碼骨架，就會退化成沒人遵守的形容詞。**代價：repo 裡會有一段時間存在關著的規則，且必須先補上 `turnedOnBy` 這條死路** |
| 35 | **不為「mono 堆疊必須含 CJK 字體」寫檢查規則** | 堆疊本身解決了 eyebrow 與表格的中文問題，不需要逐元素例外。但守住它的只剩 `tailwind.config.js` 的一段註解。`check:design` 明文只讀模板與 `site.toml`，斷言放不進去；放進 `check:classes`（它讀建置後的 CSS）技術上可行，本次選擇不做。**代價：這正是本文件開場反對的東西——一行 regression 就讓 95 個 eyebrow 在 Windows 上變細明體，而那在 Mac 上完全看不見。這是明知代價的選擇，不是疏漏** |
| 37 | header 與 footer 共用一份 `_nav-columns.html`，但 **Company 欄不共用** | 四個主題欄逐字相同，抽出來就不會再分歧。Company 欄不同**是刻意的**：footer 的品牌區塊已經有一個連到 `/` 的 TauX 字標與一個 GitHub 圖示，所以它的 Company 欄故意省略 Home 與 GitHub。把兩份強行統一會讓 footer 連兩次 `github.com/taux-io`——實測確認過。**看起來像 drift 的東西其實是一個沒有人寫下來的決定**，這條就是把它寫下來。代價：Company 欄仍是兩份，仍可能分歧，而沒有東西檢查 |
| 38 | **表格欄位標題暫不改等寬**（`th` 基礎規則試過並退回） | 看起來是乾淨的一條規則，實測撞到兩件事：`th` 的瀏覽器預設字重是 bold，套上 `font-mono` 會渲染成 **Roboto Mono 700**，而本站自架的 Roboto Mono **只有 400**——那是合成假粗體，正是字級 token 把字重寫死要防的事；而且六個 `th` 已帶 `font-display` utility，utilities 勝過 base，三十個裡只有二十四個會生效。**代價：欄位標題與 eyebrow 現在是兩種字體**，直到規範決定標題的層級由什麼承載 |
| 39 | `.tag` 保留 `font-display`，`.eyebrow` 改 `font-mono` | 同一個 `text-eyebrow` token 現在有兩種字面，這是刻意的：eyebrow 標示區段，是機器輸出；tag 標示服務名稱（`GEO Service`、`Who We Are`），是品牌聲音，與同樣留在 D-DIN Condensed 的導覽和按鈕同類。**代價：兩者並列時看起來像渲染錯誤，而沒有東西檢查這個區分** |
| 41 | ASCII 區塊的每一行必須等寬，而且要用量的 | 404 的框第一版右邊界對不齊：邊框 26 字、內容行 28 字。**用眼睛看不出來是差兩個字還是字型沒對齊**——Departure Mono 與 box-drawing 子集的 advance 差 6%，兩種原因長得一模一樣。改法是量渲染後每一行的寬度（現在五行都是 364px），不是數字元 |
| 40 | mono 堆疊裡 CJK 字體排在**拉丁等寬之後** | 排在前面的話 PingFang TC 會搶答 `→ ≥ ≈ ∞ ● ★`——它們不在 Roboto Mono 子集裡——並給出**全形** advance：`→` 在 20px 下從 12.04px 變成 20.0px，`adk-skill-patterns` 手工對齊的 ASCII 流程圖每行位移 5.6px。排在拉丁等寬之後，符號由 SF Mono 回答而字寬守住，中文仍然落到 PingFang（前面那些字體都沒有 CJK）。**副作用：這讓改動的實際影響縮小到 Windows**——Mac 上中文本來就會落到 PingFang |
| 36 | 落地切成三階段、三次合併 | 合併即上線且 CI 擋不住。階段 ① 對訪客完全不可見（只新增 token、字型與關著的規則），風險為零卻讓後兩階段有東西可依靠；階段 ② 是唯一改變視覺的一次，單獨上線才能歸因。**代價：三次 PR 的流程成本** |

---

## 附註：這一版尚未涵蓋

- 版面與間距的重新設計。本次刻意只做字體、色彩、質地與圖形，**版面、元件幾何、間距全部不動**——那些沒有規範、沒有決策紀錄、也沒有規則守著。
- 明暗模式切換：**不引入**。純黑表面保留。
- CJK webfont：**不引入**（決策 #4）。
- 點陣圖示：規範已定形狀（24×24、`crispEdges`、整數格），但本次不實作。
