# 通用語彙

這個檔案是**詞彙表，而且只是詞彙表**。不放實作細節、不放 spec、不放草稿。

## 設計語彙不住在這裡

顏色、字體、圓角、間距、元件、動態的用詞全部在 **[DESIGN.md](DESIGN.md) 的詞彙表**，那裡是唯一來源。這個檔案不重述它們一次。

理由寫在 `NOTES.md` 的開頭：這個 repo 曾經同時保留三代設計系統的描述，讀者無從判斷哪一段還有效——實際上沒有一段有效。**第二份描述會漂移**，而漂移的那份看起來和真的一樣。

---

## 詞

| 詞 | 定義 |
|---|---|
| **路由 / route** | `site.toml` 的一個 `[[page]]`。它有路徑、模板、標題、描述、canonical 與日期，而十四道閘門全部按它列舉工作 |
| **文件 / document** | `site.toml` 的一個 `[[document]]`。被產生、但不是可造訪的路由——目前只有 `404.html` |
| **長文指南 / long-form guide** | 本站的第二種頁型：一個 hero，之後是章節封面加連續正文，配一個章節索引。20 條路由裡 15 頁是這一型 |
| **閘門 / gate** | CI 會擋下合併的檢查。門檻是「乾淨」而不是「不要更糟」，所以沒有豁免清單 |
| **語意標籤 / semantic label** | 用文字承載分類，而不是用顏色。來源素材若用顏色編碼分類，改寫時分類要留下、顏色要丟掉——本站只有一個強調色，而顏色本來就不該是唯一的資訊載體 |
| **上游 / upstream** | 本站描述、但不擁有的第三方素材。上游必須在正文裡具名，而不是在頁尾。**署名為拓思的是那篇教學，不是被它描述的東西** |
| **locale** | 一個語言版本的全站。**不是「語言」**——標籤帶地區，而帶地區的是 locale。五個：`zh-Hant-TW`、`zh-Hans-CN`、`en-US`、`ja-JP`、`ko-KR`。五個一致帶地區，因為讀 hreflang 的機器會把不一致當成疏漏 |
| **locale 前綴 / locale prefix** | 每一條路徑的第一段，例如 `/zh-Hant-TW/geo-guide`。**每個 locale 都有前綴，沒有例外**——沒有 locale 住在根。**不是查詢字串**：`?lang=` 對爬蟲是同一個 URL，等於沒有做 |
| **正典 locale / canonical locale** | 一條路徑在沒有其他資訊時該去的那一個，目前是 `zh-Hant-TW`。它是**根路徑 302 的目的地**，不是「住在根的那一個」——根路徑底下沒有內容，只有轉址（決策 #58：`/` 依語言所以用 302，其餘 20 條不依語言所以用穩定的 301。這裡一度寫 301，那是實作前的計畫）。它也是 bot 在 `/` 拿到的那一份。⚠️ 這個詞取代了先前寫在這裡的「基準 locale」，那個詞假設有一個 locale 住在根，而那個假設已經不成立 |
| **x-default** | hreflang 裡「以上都不是你」的那一條。指向 `/`，也就是會依 `Accept-Language` 判斷的那個入口 |
| **bot 豁免 / bot exemption** | Googlebot 一類的爬蟲在 `/` 不被依語言導向。**豁免的是導向，不是內容**——送給 bot 的位元組必須和送給正典 locale 使用者的相同。依 User-Agent 給不同內容是 cloaking；依 User-Agent 決定跳不跳不是。這條分界寫在這裡是因為它只差一步就會被越過 |
| **字形 vs 詞彙 / script vs vocabulary** | 繁轉簡的兩件事。**字形**是固定映射，工具做得完；**詞彙**是同一個東西兩地不同名（`軟體`／`软件`、`稽核`／`审计`），要人決定。只轉字形不換詞彙的頁面是「繁體中文穿簡體字」。詞彙表在 `scripts/hans-terms.js`，那是唯一的決定點 |
| **專有名詞 / proper noun** | 名字，不是散文。機構名、法規名、登記的公司名在轉換時整個保留，只換字形——`後量子資安產業聯盟` 不會變成 `後量子網路安全產業聯盟`，因為那是引用來源的身分 |
| **切換器 / switcher** | 五個 locale 之間的連結。**是連結，不是控制項**——無 JS 必須可用，關閉時不得留在 Tab 順序裡 |

見 [[DESIGN.md 詞彙表]] 取得設計語彙；見 `NOTES.md` 取得建置、閘門與部署的現況。
